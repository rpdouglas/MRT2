# 🗺️ MRT Product Roadmap: "The 4 Waves"

**Methodology:** Strategic Waves (Prioritizing User Acquisition & Retention)

> **Spec-file note:** IDs referenced here without a `docs/projects/XX_FEATURE.md` file (PROJ-18, 19, 32, 33, 34, 35, 37, and `[BILLING]`) predate CLAUDE.md's spec-file requirement and are exempt from backfill — flagged and resolved during the 2026-07-18 governance audit. PROJ-24, 39, and 48 were removed from this list on 2026-08-04: all three now have real spec files in `docs/projects/`, restored from `docs/projects/archive/` per `ACTIVE_CYCLE.md`'s Docs Archive Cleanup entry. Any of these picked up for new work should get a proper spec at that time.

## 🌊 Wave 0: Play Store Launch Gate (Immediate)
*Added 2026-08-31 per `docs/reports/2026-08_finalreview_synthesis_and_playstore_plan.md`, reconciling the `docs/finalreview/` audit against this roadmap. Two tiers, both ahead of everything else: Tier 0 items literally block the Play Console listing; Tier 1 items are cheap and close exposure that starts the moment the app is a public download. `NEW` = no spec file yet; per `CLAUDE.md`, one is required before code, not before this roadmap entry exists.*

| Status | ID | Project Name | Persona | Description |
| :--- | :--- | :--- | :--- | :--- |
| ⏸️ **Paused 2026-09-03** | `PROJ-07` | **Play Store TWA — Sprint 9.2 completion** | All | Paused by user choice to focus on marketing materials — not blocked. TWA built/signed/uploaded to Internal Testing (release 6, v1.9.11), device-verified working (a real `www`-subdomain Digital Asset Links redirect bug was found and fixed along the way). App-content checklist (11/11), Health apps declaration, and most of Store Listing done live in Console. **Resume checklist:** Content rating questionnaire (not started), confirm remaining Data Safety categories saved, the Subway Test. 2026-09-03 policy research found this account (Organization type) is exempt from Google's "12 testers/14 days" closed-testing gate — Internal Testing → Production directly. See `docs/projects/07_PLAY_STORE_TWA.md` and `docs/PLAY_STORE_BUBBLEWRAP_GUIDE.md` Phase 9. |
| 🟡 **In Progress** | `NEW` | **Play Console Data Safety Questionnaire** | All | Tier 0 — top-level questions and several categories entered live in Console 2026-09-03 per the drafted answers at `docs/legal/PLAY_STORE_DATA_SAFETY_DRAFT.md` (including the "Health info" categorization decision). Remaining categories (Personal info, Financial info, Device/other IDs, Location) not yet confirmed saved — paused alongside `PROJ-07`. |
| ⚪ Planned | `NEW` | **Play Console Content Rating Questionnaire** | All | Tier 0 — not started. Prep guidance (expected answers per category, likely "Teen" outcome given substance-reference content) written up 2026-09-03 in `docs/PLAY_STORE_BUBBLEWRAP_GUIDE.md` Phase 9 — paused alongside `PROJ-07`. |
| 🟡 **Code complete, blocked** | `PROJ-105` | **Google Play Billing (Digital Goods API)** | All | Deployed to prod 2026-09-02. Blocked on Play Console payment-method verification clearing (account-side, no ETA) before the `premium.monthly` subscription product can be created. A dev/emulator-only mock purchase path (2026-09-03) lets the full flow be tested meanwhile — see `docs/projects/105_PLAY_BILLING_TWA.md`. |
| ✅ **Done** | `NEW` | **Store Listing Visual QA** | All | Tier 0 — done 2026-08-31. Found `public/Marketing/Screenshots/` (the actual Play Store listing set, per the production readiness audit's own citation) hadn't been touched since 2026-04-21 — predates almost the entire current UI (Recovery Games, Games Hub, Nav Tab-Bar Unification, Tools Hub Regrouping, Workbook Marketplace, Sobriety Hero themes, Profile UX Remediation, and more). Regenerated via the `PROJ-63` pipeline (`npm run screenshots:generate`) and refreshed: the 6 images actually live on `Welcome.tsx` (the public landing page — one of which, `scn_dashboard_02_clean_time`, was silently serving a 985KB unoptimized PNG instead of the 77KB WebP next to it, a confirmed `scripts/generate_asset_index.py` key-collision bug when `.png`/`.webp` siblings share a basename), plus a new curated 8-image Play Store listing set (Dashboard, Journal, Recovery Games hub, Urge Surfer, Workbooks, Goal Ladder, Daily Crossword, SOS Crisis modal). Removed 9 redundant unoptimized PNG duplicates. **Found and fixed along the way:** `lisa-sponsees.webp` was actually a screenshot of the *internal Admin Dashboard*, mislabeled with a fully fabricated "Sponsor Sponsee Directory" description in `docs/SCREENSHOTS_INDEX.md` — that feature doesn't exist (`PROJ-05` is paused, no UI built). Renamed to `admin-users-directory.webp`, description corrected, generator script relabeled with a comment against reuse. Regenerating `src/data/assets.ts` via its own generator script also fixed 8 pre-existing dead links to `public/Chips/*.png` files deleted under `PROJ-98` but never reflected in the checked-in index. `npm run check` clean throughout. |
| 🟡 **In Progress** | `PROJ-103` | **Firebase App Check** | All | Tier 1 — audit P0 #2. Spec + Rule-of-3 strategy (Strategy B: functions enforced + Firestore/Auth Monitor mode) done and approved 2026-08-31; client-side init shipped same day (inert — no site key configured in any environment yet). Blocked on 2 Firebase Console steps only a human can do (reCAPTCHA site key registration per environment, debug token registration for CI/dev) before `enforceAppCheck` can safely go on the 3 callable functions without breaking `vault.spec.ts` and real vault unlocks. See `docs/projects/103_FIREBASE_APP_CHECK.md` §7. |
| ⚪ Planned | `NEW` | **GCP Cost/Budget Alerting** | All | Tier 1 — console-only action, already flagged as not done in `docs/projects/99_FIRESTORE_BACKEND_HARDENING.md` §4. |
| ✅ **Done** | `NEW` | **Sub-Processor / DPA Disclosure List** | All | Tier 1 — done 2026-08-31: `docs/legal/SUB_PROCESSORS.md` (5 processors, code-verified against actual call sites) and `docs/legal/PLAY_STORE_DATA_SAFETY_DRAFT.md` (draft answers, ready to transcribe once Play Console unblocks). **Found along the way:** `docs-site/privacy.md` (the live, linked policy) and `docs/legal/PRIVACY_POLICY.md` had both drifted to list only Firebase + Gemini as third parties — PostHog, Stripe, and Google Drive were missing from both despite real data flows. Both fixed. The two files had also drifted from *each other* independently — worth a proper reconciliation pass at some point, not done here (out of scope for this chore). |
| 🟡 **Mostly Done** | `NEW` | **Pre-Launch Chores** | All | Tier 1. **2026-08-31:** `/debug` now gated behind `isAdmin`; devcontainer aligned to Node 24; `.eslintrc.json`/`vite.config.bak` removed; the `react-router-dom` HIGH finding turned out to be an already-documented accepted exception (non-RSC app, see `docs/RUNBOOK.md`) — a separate, newly-surfaced `dompurify` moderate finding was fixed instead via an `overrides` pin. **Still open:** actually reviewing the existing `web-vitals` → PostHog dashboard data — needs a human with PostHog account access, not doable from this environment. Full detail in `docs/ACTIVE_CYCLE.md` Chores & Tech Debt. |

## 🌊 Wave 1: Acquisition & Friction (Weeks 1–6)
*The immediate goal: Stop users from abandoning the app on Day 1 by removing the "Security Tax."*

| Status | ID | Project Name | Persona | Description |
| :--- | :--- | :--- | :--- | :--- |
| ✅ **Shipped** | `PROJ-41` | **The Dynamic Anchor** | David / Ned | A slim, frictionless, 2-column Quick Action Bar replacing the static daily pledge. |
| 🟡 **In Progress** | `PROJ-07` | **Play Store TWA** | All | Generate assetlinks.json and finalize Google Play Store deployment. Sprint 9.1 implemented 2026-07-19 (manifest hardening, mobile CSS, legal footer links, `/delete-account` route) — code-verified, not yet device-tested. Sprint 9.2 (Bubblewrap, submission) unblocked 2026-08-31 — Google Play Console account verification (website + identity/phone) confirmed complete; both prior blockers (PROJ-67, PROJ-68) are also resolved. See Wave 0 above for the full submission checklist and `docs/projects/07_PLAY_STORE_TWA.md`. |
| ✅ **Shipped** | `PROJ-67` | **Signing Key & Secrets Hygiene** | All | New keystore generated and stored in Google Secret Manager; `assetlinks.json` updated; old compromised keystore purged from all git history and force-pushed (2026-07-19). See `docs/projects/67_SIGNING_KEY_SECRETS_HYGIENE.md`. |
| ✅ **Shipped** | `PROJ-68` | **Gate Stripe Checkout Out of the Android TWA** | All | In-app Stripe purchase flow hidden specifically inside the Play-installed TWA (`isAndroidTWA()` referrer check), sidestepping the jurisdiction-dependent Play Billing policy question rather than resolving it. See `docs/projects/68_STRIPE_TWA_GATING.md`. |
| ✅ **Shipped** | `PROJ-65` | **Vault PIN Brute-Force Hardening** | All | Rate-limited server-pepper key derivation (multi-device-safe alternative to the originally-proposed IndexedDB Master Key, which was rejected) so a Firestore breach doesn't reduce to a 10,000-combination PIN search. Implemented 2026-07-17, external security review passed 2026-07-19 — see `docs/projects/65_VAULT_KEY_HARDENING.md`. |
| ✅ **Shipped** | `PROJ-73` | **Test Suite Hardening — Vault-PIN Pepper Coverage** | All | Closes the coverage gap left by PROJ-65 so the peppered PIN-derivation scheme is actually exercised by automated tests at every layer (Cloud Function handler, client orchestration, browser e2e). Phases 1-3 shipped; Phase 4 (stretch — Recovery Games) component-test coverage shipped 2026-07-22, and Subway Test (PROJ-72's browser QA gap) automated 2026-07-23 as a real Playwright e2e spec. All QA checklist items closed. See `docs/projects/73_TEST_SUITE_HARDENING.md`. |
| ⚪ Planned | `NEW` | **Legacy Vault-Key Migration** | All | Force or strongly nudge pre-`PROJ-65` accounts to rotate their PIN onto the peppered `usesPepperV2` scheme. Audit P0 #3 — a live, not historical, cryptographic weakness (`docs/finalreview/15_RISK_MATRIX_AND_TECH_DEBT_REGISTER.md` `TD-01`). Not submission-blocking, but the top post-launch priority. Needs a spec via `/planning`. |
| ⚪ Planned | `NEW` | **Firestore Validation Extension** | All | Extend the `journals`/`game_saves` shape/size-validation pattern to the remaining 4 sensitive collections (`workbook_answers`, `service`, `rosc_assessments`, `game_progress`) — explicitly scoped down in `PROJ-99`, now picked back up. `TD-03`. Needs a spec via `/planning`. |
| ⚪ Planned | `NEW` | **Account Deletion Full-Purge Completion** | All | `executeTotalAccountAnnihilation()` (shared by `/delete-account` and Profile → Data's Account Deletion) doesn't scan/delete `service/{id}` (sponsee notes), `users/{uid}/rosc_assessments`, `client_errors`, or Stripe/Play-Billing subcollections under `users/{uid}` — found while drafting `docs/screens/delete-account.md`/`profile/data.md` during the marketing-docs pass (2026-09-03). Notably, Reset Vault's separate `executeCryptoShredding` **does** correctly delete `rosc_assessments`, so the two destructive flows have inconsistent coverage of the same collection today. Security/compliance-sensitive — needs a spec + `zk-audit` pass via `/planning`, not an ad hoc fix. |
| ⚪ Planned | `NEW` | **Close Remaining Uncapped AI Cost Gap (CBT Coaching / CBA Reflection)** | All | `generateCBTCoachingPrompt` (`cbt_coaching_prompt`, `GuidedWorkflowEngine.tsx`) and `generateCBAReflection` (`cba_reflection`, `CBATool.tsx`) are premium-gated **client-side only** — `functions/src/index.ts`'s `generateAIInsights` has no tier check or rate limit for either `analysisType`. Same class of gap `PROJ-106` already closed for `workbook_analysis`/`workbook_coach`/`audio_analysis` (its `checkCooldown`/`checkFloor` helpers are directly reusable), but `PROJ-106` was explicitly scoped to those 3 flows only — this is a separate, previously undiscovered gap, found while drafting `docs/screens/tools/README.md`/`tools/cba.md` (2026-09-03). Needs a spec via `/planning` (a natural `PROJ-106`-style follow-up, not a new ticket from scratch). |
| ⚪ Planned | `NEW` | **Structural Encrypt-Before-Write Enforcement** | All | Typed wrapper or custom lint rule in `useFirestoreCrud.ts` so the zero-knowledge boundary is structurally enforced, not convention-only. `TD-02`. Needs a spec via `/planning`. |
| ⚪ Planned | `NEW` | **Global Reduced-Motion/Focus-Visible CSS Baseline** | All | Currently handled per-component (2 explicit checks found); add a single global `prefers-reduced-motion`/`focus-visible` CSS baseline. |
| ⚪ Planned | `NEW` | **Tools Hub Stub/Hidden-Content Decision** | All | One product decision, two parts: build-or-remove the permanent-looking "SMART Goal" stub card (`TD-17`), and document reactivation criteria (or a permanent-hide decision) for the two soft-hidden games, Craving Buster and Thought Challenge (`TD-16`). |
| ⚪ Planned | `NEW` | **MAT Dose-Tracking & Discreet Notifications (Jordan)** | Jordan | Build what `docs/PERSONAS.md` documents for Jordan but doesn't exist in code: dose-adherence tracking, a customizable sobriety-counter label (e.g. "Days of Stability" instead of a fixed counter), and discreet, drug-name-free lock-screen notifications. Promoted 2026-09-03 from `docs/BACKLOG.md`'s "Jordan discreet-UI feature depth audit" — its trigger ("before marketing that targets the MAT/Jordan segment") fired while drafting the `docs/marketing/` persona briefs, which confirmed the gap directly (no counter-label prop on `SobrietyHero.tsx`; no MAT/dose-specific code anywhere in `src/`). Needs a spec via `/planning`. |

## 🌊 Wave 2: Retention & Community (Weeks 7–16)
*The secondary goal: Keep users past Day 30 through peer support and shame-free resets.*

| Status | ID | Project Name | Persona | Description |
| :--- | :--- | :--- | :--- | :--- |
| ⚪ Planned | `NEW` | **Privacy-Preserving Community** | All | Opt-in, pseudonymized social feed moderated by Gemini. (Requires heavy Zero-Knowledge schema design). |
| ⚪ Planned | `PROJ-35` | **The Autopsy Engine** | David | A shame-free CBT reset flow that captures triggers immediately following a relapse. |
| ⚪ Planned | `NEW` | **Multi-Addiction Clocks** | All | Tracking multiple habits/substances simultaneously. |

## 🌊 Wave 3: Platform Maturity & Sponsors (Weeks 17–26)
*The third goal: Capture the "Lisa" (Sponsor) and "Walt" (Long-term) demographics.*

| Status | ID | Project Name | Persona | Description |
| :--- | :--- | :--- | :--- | :--- |
| ⏸️ **Paused** | `PROJ-05` | **The Service Network** | Lisa | Encrypted Sponsee Rolodex. (Paused to focus on Wave 1 Onboarding). Its Dashboard "Coming Soon" placeholder slot is being reassigned to `PROJ-72` Recovery Games — when unpaused, this needs a new entry point (e.g. a nav item) instead. **2026-08-31 note:** the finalreview audit calls this the single highest-leverage recommendation in the entire report, and Wave 1 (the reason it was paused) is now essentially done — see `docs/reports/2026-08_finalreview_synthesis_and_playstore_plan.md` §5. Flagged, not actioned; status stays Paused pending a future planning session. |
| ⚪ Planned | `PROJ-33` | **Predictive Relapse Engine** | Walt / Lisa | AI analysis of Insights collection to generate proactive warning tasks. |
| ⚪ Planned | `PROJ-34` | **Aggregated Stats Engine** | All | Cloud Functions to calculate stats on-write to reduce Firestore read costs. |

## 🌊 Wave 4: Enterprise & Ecosystem (Weeks 27–52)
*The final goal: Defensible technical moats and B2B expansion.*

| Status | ID | Project Name | Persona | Description |
| :--- | :--- | :--- | :--- | :--- |
| ⚪ Planned | `PROJ-37` | **Secure Handshake Protocol** | Lisa | Local QR-code generation to share encrypted 4th-step inventory directly to a sponsor. |

## ✅ RECENTLY SHIPPED
* `PROJ-97` Dependency Hygiene Round 2 (Replaced `@use-gesture/react` with native touch handlers in `SwipeableTaskRow.tsx` — measured 20KB raw / 6.5KB gzip off the vendor chunk — plus a new 8-test suite that didn't exist before; deleted verified-dead `knip` findings and dropped the `export` keyword from 4 more that turned out to be used internally, not dead; added `eslint-plugin-jsx-a11y` and triaged all 48 findings it surfaced (fixed ~40, suppressed the rest with documented reasoning, e.g. Tailwind's `role="list"` fix for Safari/VoiceOver, and 7 deliberate modal-focus `autoFocus` uses). `crossword-layout-generator` replacement descoped to a trigger-based `BACKLOG.md` item — zero CVEs, no forcing function, high rewrite risk for a working feature.)
* `PROJ-101` Data Access Layer Consolidation — Round 2 (Data Access Layer Consolidation Round 2 — finished the useFirestoreCrud migration PROJ-59 started: migrated useJournalOperations/useWorkbookAnswers/useTaskOperations onto its shared query/mutation primitives (extending the mutation primitive with an optional onSuccess hook along the way), removed useTaskOperations' dead optimistic-update machinery after finding it targeted a cache key the real Tasks page never reads, and added the first direct AuthContext unit tests (isAdmin claim/role-fallback logic, the DEV-only mockUser bypass, onAuthStateChanged's null-user and profile-fetch-failure paths).)
* `PROJ-100` AI Prompt Safety Hardening (AI Prompt Safety Hardening — closed the AI-safety gaps in generateAIInsights: real payload schema validation replacing presence-only checks and blind as-casts (also closing a latent unvalidated-scope read in the rate-limit branch), prompt-injection delimiting across all nine analysisType flows, and dual-profile Gemini safetySettings (permissive for the nine user-content flows given this app's crisis-journaling subject matter, stricter for the two editorial content-generation calls).)
* `PROJ-99` Firestore & Backend Hardening (Closed the Firestore/Cloud-Functions-layer findings from the production-readiness audit: added document-shape/size validation to firestore.rules (journals/game_saves), the repo's first Firestore rules unit test suite (22 tests via a real emulator), 2 missing composite indexes, Cloud Functions cost guardrails (maxInstances on generateAIInsights/dailyBeacon) plus a real dailyBeacon sendEach 500-message correctness fix, and a safe-migration telemetry signal ahead of eventually converging the admin-definition fallback.)
* `PROJ-98` Audit Quick-Win Remediation Sweep (Closed 10 quick-win findings from the production-readiness audit: PWA install size cut ~82%, 15 accessibility labels added, 3 previously-undocumented AI call sites reviewed and approved into the governance list, functions/ dependency vulnerabilities reduced, and context re-render performance hardened.)
* `PROJ-96` CI/Deploy Operational Safety Net (Closed the CI/deploy operational safety net gap: added hard-fail production-dependency vulnerability gates to the verify job (root + functions/ workspaces, each scoped to its own lockfile), remediated 2 critical + 4 high vulnerabilities in functions/ that no prior audit had ever covered, and wrote docs/RUNBOOK.md covering Hosting/Functions/rules rollback procedures and the gate's one documented exception (a non-applicable react-router RSC-mode CVE).)
* `PROJ-95` Journal Read-Cost Reduction (Dashboard Anchor Status + Journal History) (Fixed useAnchorStatus.ts (used by Dashboard on every app open) doing an unbounded full-history journals read just to check if any entry exists from today - scoped to a date-range query with its own distinct cache key (critical: it previously shared a key with JournalInsights.tsx/AchievementsTab.tsx, which genuinely need full history). Restructured JournalHistory.tsx from a single unbounded fetch to two modes - current-year-only by default, explicit opt-in full-history for the AI Wizard/search-everything/browsing prior years - after finding the originally-planned per-year incremental loading had a real discoverability flaw. Updated the user guide to match.)
* `PROJ-94` Production Error Telemetry Hardening (Fixed ErrorBoundary's guaranteed-to-fail Firestore write when unauthenticated (PostHog capture already covered this case). Added trackMutationFailed telemetry to the real leverage points: the shared useFirestoreMutation wrapper's onError (covers 3 files), all 4 of useTaskOperations' hand-rolled mutations (previously zero visibility of any kind on a failed task write), and useJournalOperations' 3 mutations (previously duplicated ad-hoc across 3 calling components). Added trackClientError for 8 genuinely scattered console.error sites in AuthContext/EncryptionContext. Added web-vitals reporting. All new telemetry call sites verified to pass only domain + error name, never decrypted content.)
* `PROJ-93` Journal Template Moment-Based Grouping (Regrouped the journal template picker from clinical-modality groups to moment-based sections (In the Moment / Daily Rituals / Reflection & Insight), rendered as a collapsible bottom-sheet accordion; each template still shows its modality as secondary metadata.)
* `PROJ-92` Performance & Scale Polish (Added explicit gcTime (30 min) to the QueryClient defaults, previously unset. Removed lucide-react entirely, consolidating onto @heroicons/react across 4 files (the dependency audit that flagged this missed one of the 4 files and undercounted FeedbackViewer.tsx's icons by 3). Verified the rest of the originally-planned scope (list virtualization, PWA precache, Firestore read/index audits, asset sizes) was already fine on inspection — documented rather than touched.)
* `PROJ-91` WCAG 2.2 AA Accessibility Remediation (Fixed 4 real accessibility violations found by direct code review (icon-only buttons missing aria-labels, a keyboard-inaccessible popover) plus 3 more found only by a new automated axe-core scan (disabled pinch-zoom app-wide, an unlabeled Dashboard menu button, a contrast failure on the Backup Needed banner) — disproving the audit's original theme.ts contrast hypothesis in the process. Added e2e/golden-paths/a11y.spec.ts as a permanent CI regression gate.)
* `PROJ-90` Security, Dependency & Compliance Hardening (Ran npm audit fix (non-force) to clear the ws/postcss/react-router-dom vulnerabilities, removed 8 stray console.log statements and added an esbuild.pure guard against future ones, added a Play Store health-app medical disclaimer to Login and Welcome, and added a production-build e2e regression test proving the mockUser bypass stays fixed.)
* `PROJ-89` Play Store Release Blockers (Fixed the mockUser client-side auth bypass (gated behind DEV-only builds) and split the 1.83MB monolithic vendor bundle into targeted chunks — the two hard blockers from the Production Readiness Audit before Play Store submission.)
* `PROJ-87` Trigger Match & Knowledge Quests Immersive Shell Restyle (Restyled Trigger Match and Knowledge Quests to match the dark-immersive shell used by Goal Ladder/Jeopardy, via an opt-in theme prop on the shared quiz component so the still-light Thought Challenge is unaffected.)
* `PROJ-88` Daily Crossword Immersive Shell Restyle (Restyled Daily Crossword to match the dark-immersive shell used by Goal Ladder/Jeopardy/Trigger Match/Knowledge Quests, and fixed a mobile on-screen-keyboard bug where typed letters weren't reaching the grid.)
* `PROJ-86` Recovery Jeopardy Immersive Shell Restyle (Extended the Goal Ladder-style dark-immersive shell (background, header score chip, footer) to Recovery Jeopardy across every round, replacing GameShell's light chrome for this game.)
* `PROJ-85` Goal Ladder Immersive Redesign & Hero-Derived Dashboard Theming (Rebuilt Goal Ladder as a self-contained dark-immersive screen with a literal SVG rung-climb visual, and made Dashboard's header/background derive from the user's chosen Sobriety Hero color.)
* `PROJ-84` Dashboard Bento Tile Refresh & Tasks Contrast Fix (Componentized the six dashboard tiles into a THEME-driven BentoCard, and darkened THEME.tasks.header one shade for white-text legibility.)
* `PROJ-83` Sobriety Hero Typography Scale-Up (Scaled up Years/Months/Days, Total Days, and Saved text on the Dashboard sobriety hero by one Tailwind type step.)
* `PROJ-82` PostHog Telemetry Expansion & Zero-Knowledge Alignment (PostHog Telemetry Expansion — safe, ZK-guarded analytics across crisis modal, urge surfer, recovery games, somatic vitality, and React error boundary.)
* `PROJ-81` Games Hub Design System Alignment (Corrected PROJ-80 drift by replacing GamesHub's one-off back-button header with the shared VibrantHeader component and retinting its dark hero card to match the Dashboard's "My Games" bento tile, restoring the always-available SOS button.)
* `PROJ-80` Games Hub Unified Hero Restyle (Restyled the Recovery Games hub into one unified dark card with a flat game list (replacing the tile grid), delisted Craving Buster/Thought Challenge from it without touching their routes or components, and surfaced Thought Challenge as a new Tools Hub entry so it isn't left unreachable.)
* `PROJ-79` Daily Crossword (Added Daily Crossword, the 8th Recovery Games entry — a server-generated, non-personalized daily puzzle persisted like every other game but deliberately excluded from XP, matching its reward-free design.)
* `Maintenance` Build-Process Tooling Gap Closure (Added dependency-audit, secrets-scanning, ingestion, debt-ledger, and release-scribe skills/hooks to close gaps between the documented Recursive Build Protocol and the actual `.claude/` tooling.)
* `PROJ-78` VibrantHeader Right-Column Overflow (VibrantHeader Right-Column Overflow — fixed a layout bug where the header's shrink-0 center title/subtitle column never yielded width, pushing the SOS button up to 29px past the viewport edge on pages with a long subtitle (worst case: Workbooks at 412px/320px). Fix: center column now min-w-0 with a truncating subtitle; SOS's own column deliberately untouched so it can never shrink.)
* `PROJ-77` Nav Tab-Bar Unification (Nav Tab-Bar Unification — extracted a shared TabBar component and migrated Journal, Vitality, Workbooks, Tasks, and Profile onto it, sourcing per-feature colors from theme.ts. Fixed a real overflow/overlap bug at narrow viewports found during QA (min-w-fit + overflow-x-auto).)
* `PROJ-76` Gamification Dashboard Relocation (Moved the Rank/Level/XP display and every bento tile's stat numbers off the Dashboard into a new "Achievements" tab on Profile, so the Dashboard reads as a calm core-recovery snapshot rather than a gamified one — no gamification logic deleted, just relocated.)
* `PROJ-73` Test Suite Hardening — Vault-PIN Pepper Coverage (Closes the coverage gap left by PROJ-65 so the peppered PIN-derivation scheme is actually exercised by automated tests at every layer (Cloud Function handler, client orchestration, browser e2e). Phase 4 (stretch — Recovery Games) component-test coverage shipped 2026-07-22, and the Subway Test (PROJ-72's browser QA gap) automated 2026-07-23 as a real Playwright e2e spec. All QA checklist items closed.)
* `PROJ-75` Workbook Marketplace (v1 — Official Catalog) (Added workbook marketplace v1 — Marketplace tab lets users add/remove official workbooks from their library via a new installedWorkbookIds profile field, no new Firestore collection or rules.)
* `PROJ-74` Legacy Vault Unlock Hang on Decrypt-Mismatch (Legacy Vault Unlock Hang — a decrypt mismatch during legacy (pre-pinVerifier) vault unlock now fails closed with a clear error instead of hanging the unlock button indefinitely.)
* `PROJ-72` Recovery Games (Zero-knowledge, anti-shame persona-targeted mini-games and psychoeducation tools layered on the existing XP/streak system, replacing the Dashboard's dead "Service — Coming Soon" tile. All 7 phases shipped: Craving Buster (David), Morning Intent, Recovery Jeopardy (group activity), Fast Lane (Walt, a multi-week economic life-sim), Goal Ladder (Ned), Thought Challenge (Lisa), Trigger Match (Walt), and Knowledge Quests (general psychoeducation, decoupled content packs) — closed out by extending the existing data-export/deletion engine to cover Recovery Games data and adding Milestone Image sharing to Fast Lane/Jeopardy win screens.)
* `PROJ-30` Data Sovereignty Engine (Local decryption and structured export (JSON/PDF) via `DataExportPanel.tsx`/`exporter.ts`, plus Google Drive auto-sync and account deletion. This row was stale — still `⚪ Planned` in Wave 4 despite the engine being fully built; corrected while extending it to cover Recovery Games data for `PROJ-72`.)
* `Maintenance` SMART Tool Journal Entries (Journal History previously rendered completed SMART Tool sessions as raw JSON text. Now reuses the existing Tool History rendering pattern — a tool badge, one-line headline, and expandable humanized field list — in the main Journal History timeline, and fixed search/share to use humanized text for these entries. Unfinished drafts and the (now-unsafe) Edit button no longer show for tool entries; AI analysis (JournalAnalysisWizard, Deep Pattern Analysis) also reads humanized text instead of raw JSON.)
* `PROJ-71` Tools Hub Regrouping (Regrouped the flat Tools Hub into four moment-based collapsible accordion sections (Right Now / Before It Happens / After a Hard Moment / Big Picture) with Right Now expanded by default, added a resume-draft callout, and brought the page onto the Momentum Kinetic visual system via a new tools GlassCard variant.)
* `PROJ-23` The QA Sentinel (Playwright E2E golden-path suite (Gate/Vault/Ledger) wired into CI as a blocking pre-deploy gate against local Firebase emulators. Found and fixed a real journal-entry data-loss race condition along the way, plus three CI-only environment gaps (firebase-tools, Java 21, Firebase client env vars) invisible in local dev.)
* `PROJ-70` Changelog Historical Scrub (Rewrote all remaining changelog entries (v1.0.0-v1.8.18) into plain user-facing language: stripped PROJ-ID tags, file/hook/component names, and engineering jargon; deleted or trimmed entries with zero user-facing content (admin-only tooling, pure test/architecture work).)
* `PROJ-69` Changelog Split — Public/Internal Separation (Split the public docs-site changelog from internal engineering detail: scrubbed two live security disclosures and rewrote five mislabeled entries, added a leak-guarded --public-note/--version path to sync_ticket_docs.py, and added a Check 0 user-visible classification gate to the ticket-close skill.)
* `PROJ-68` Gate Stripe Checkout Out of the Android TWA (Decided against implementing full Google Play Billing — sidesteps the jurisdiction-dependent Payments policy question entirely by hiding the in-app Stripe purchase flow specifically inside the Play-installed TWA, via a new `isAndroidTWA()` referrer check. Existing subscribers can still manage/cancel from inside the app; only new purchases are gated. Unblocks PROJ-07 Sprint 9.2.)
* `PROJ-65` Vault PIN Brute-Force Hardening (Rate-limited server-pepper key derivation so a Firestore breach doesn't reduce to a 10,000-combination PIN search — multi-device-safe alternative to the originally-proposed IndexedDB Master Key. Implemented and manually verified end-to-end 2026-07-17; external security review passed 2026-07-19, closing the last open item.)
* `PROJ-67` Signing Key & Secrets Hygiene (Rotated the Android app-signing keystore after discovering it was tracked in git — new keystore generated and stored in Google Secret Manager, `assetlinks.json` updated, old keystore purged from all git history via `git filter-repo` and force-pushed to `origin/main`, verified clean both locally and on the remote. Found in `docs/reports/archive/2026-07_app_readiness_review.md` §1.)
* `PROJ-66` User Guide Relabel Sync (Brought the published docs-site user guide and its dev spec into alignment with the in-app "My X" navigation labels from commit `a05a24f` — sidebar nav, page H1s, freemium bullets, and `docs/specs/12_USER_GUIDE.md`'s architecture description corrected. Found via the 2026-07-18 governance audit as a spec with no prior ROADMAP/ACTIVE_CYCLE reference.)
* `PROJ-64` Gemini AI Proxy & Platform Hardening (Cloud Functions proxy removes the client-exposed Gemini API key, server-side AI rate limiting, Firestore persistent offline cache, PostHog decryption-failure telemetry — shipped 2026-07-13 in commit `6748388`, spec backfilled 2026-07-16 after a governance audit found the shipping commit's message overstated its scope; the key-derivation hardening it also claimed was never actually implemented and is tracked separately as `PROJ-65`.)
* `PROJ-63` Mobile Screenshot Generator (Implement automated mobile screenshot generator with persona-based mock bypass.)
* `PROJ-62` Tech Debt Quick Wins — Logging, Rules, Duplicate Reads, `any` Suppressions (Cleared five deep-review quick-win chores: removed a debug console.log, hardened the CI service-account heredoc, tightened ai_logs/client_errors/feedback Firestore rules to uid-match, extracted Tasks.tsx's raw onSnapshot into useTasksList, and fixed the last four any-type suppressions (two of which were masking real latent bugs).)
* `Maintenance` Journal Sharing Format (Appended `myrecoverytoolkit.ca` to the end of the plaintext journal entry share text and added screen reader accessibility title to the Share button.)
* `PROJ-61` Test Coverage Backfill — ZK-Adjacent & Firestore-Write Paths (Backfilled test coverage for exporter.ts, useROSCAssessments, useRateLimits, and functions/prompts.ts prompt-construction logic.)
* `PROJ-60` God File Decomposition — Vitality & Data Management (Split Vitality.tsx and DataManagement.tsx along independent concerns, isolating account deletion and fixing a ZK plaintext-write bug in Vitality journal saves.)
* `PROJ-59` Data Access Layer Consolidation (Consolidated Firestore data-access layer onto TanStack Query: fixed two stale-cache bugs, extracted a shared query/mutation factory, migrated all raw CRUD call sites.)
* `PROJ-58` Profile UX Remediation (Unified three incompatible save models into one autosave pattern; fixed a TanStack Query bypass that could desync Dashboard badges after a Profile save; restyled the vault-reset confirmation into a Headless UI dialog; made Security/Data tabs deep-linkable routes; cause-specific import-failure messages; distinct partial-failure state for the displayName auth sync; 44px touch targets)
* `PROJ-57` Journal Template Modality Expansion (Default journal template picker expanded from 4 Twelve-Step-only templates to 15 templates across 11 recovery modalities — CBT/SMART, DBT, Mindfulness, Harm Reduction, Reset, Trauma-Informed, ACT, Motivational, MAT, General — grouped by modality; new templates render as guided multi-prompt forms reusing the existing custom-template rendering path)
* `PROJ-56` Sobriety Hero Color Themes (Swatch button on the dashboard hero plus a matching "Hero Appearance" picker in Profile → General; 5 curated presets stored as a plaintext `heroColor` field on `users/{uid}`, defaulting to today's amber look when unset.)
* `PROJ-55` Workbook Remediation (Real, step-specific content for 12-Step Steps 2-11 with unique literature-grounded contexts; migrated Workbook pages off direct Firestore calls onto a TanStack Query hook; fixed a fragile decryption heuristic; removed orphaned data module; dynamic gamification denominator)
* `PROJ-50` Guided CBT/REBT Interactive Workflows (Step-locked guided flows for ABCDE, CBA, and DENTS Scenario Mode; new Thought Record and Five Questions tools; AI coaching prompts; Tools Hub redesign with Start Fresh/Resume/History entry points and completion tracking)
* `PROJ-54` Journal Insights Momentum UI Redesign (GlassCard, dark theme, smooth charts, and typography upgrade)
* `PROJ-53` ROSC Matrix Visual Upgrade (Pill Capsules) (Animated segmented pill visualization, glassmorphic dark theme, replacing the Recharts radar chart)
* `PROJ-49` The Recovery Capital (ROSC) Matrix (SAMHSA domain assessment, monthly for free / weekly for premium; full-screen route with Recharts longitudinal trend charts; ZK-encrypted AI narrative and suggested actions; free-tier self-report scores)
* `PROJ-47` The Ledger — Precision, Resilience & Tab Redesign (Monthly day-drift fix, grace window, missedCountHistory, Today/Later/Log tabs)
* `PROJ-48` User Guide Synchronization Sprint (Daily Readings page, Dynamic Anchor, Voice-to-Vault, Recurrence Table, Smart Reset expansion)
* `PROJ-46` The Ledger — Frictionless Task Module Upgrade (Swipe Gestures, Quick Capture, Rhythm Score, AI Context Cards)
* `PROJ-42` Daily Readings Engine (Multi-Modality Content)
* `PROJ-41` The Dynamic Anchor (Circadian Companion Widget) — ships as a 2-card Check-In/Reading bar; the spec'd 3rd "Intent" card was descoped and its dead code removed, see docs/projects/41_DYNAMIC_ANCHOR.md §6.
* `PROJ-40` Test Suite Audit (Vitest Pipeline Overhaul)
* `PROJ-39` Deferred Vault Lock (Frictionless Onboarding)
* `PROJ-31` Crypto Chunking Pipeline (Zero-Knowledge Scaling)
* `PROJ-26` The Beacon (Push Notification Engine) — server-scheduled Web Push (FCM) for sobriety milestones and overdue-habit reminders; spec backfilled 2026-07-09 as part of the notification-system remediation.
* `PROJ-17` Changelog Beacon (Dashboard toast comparing a build hash against what the user last saw) — shipped 2026-03-17 but had been silently non-functional since 2026-02-17 due to an unwired build-manifest generator; found and fixed 2026-07-16 as part of a governance audit, spec backfilled at the same time — see `docs/projects/17_CHANGELOG_BEACON.md`.
* `PROJ-19` The Landing Page (Vibrant Momentum & Persona Showcase)
* `PROJ-24` The Asset Engine (Strict-Typed Image Dictionary)
* `PROJ-18` Command Center (AI Telemetry Dashboard & SRE Rate Limiting)
* `[BILLING]` Stripe Webhook & Premium Provisioning Pipeline
* `PROJ-32` The Viral Export Engine (AI Insight Milestone Cards)
