# 🏃 Active Development Cycle

**Current Phase:** Cycle 2026-W30 (Sprint 9.9)
**Methodology:** ISO Year-Week Continuous Delivery

## 🚨 Triage & Hotfixes (Priority 1)
*Issues bypassing the backlog to protect user retention.*
- [x] **Overdue Task Completion Fix:** Fixed a bug where overdue recurring tasks calculated their next due date relative to their past due date instead of today.
- [x] **PROJ-26 Hotfix:** FCM SW token migration — fixed notification click-through (absolute URL) and stale token re-registration on SW version bump.
*(Queue Empty)*

## 🛠️ Active Projects (Priority 2)
*Core feature work for the current cycle.*
*(Play Store submission (`PROJ-07`) and Play Billing (`PROJ-105`) moved to ⏸️ Paused below, 2026-09-03 — see that section for the resume checklist. Marketing materials work begins this cycle in their place.)*

## 📋 Queued — Needs a Spec via `/planning` Before Work Starts
*Real feature/security work surfaced by the 2026-08-29 finalreview audit, phased into `docs/ROADMAP.md` Wave 0/Wave 1 (2026-08-31) — but per `CLAUDE.md`, none of these start until a `docs/projects/XX_FEATURE.md` spec exists. Not chores; don't pick these up ad hoc.*
- ~~**Firebase App Check** (Wave 0, Tier 1)~~ **Spec'd, strategy approved, client-init shipped 2026-08-31** — see `docs/ROADMAP.md` Wave 0 and `docs/projects/103_FIREBASE_APP_CHECK.md` §7. Blocked on 2 Firebase Console steps before the remaining code (enforcement flags, e2e debug-token wiring) can safely land.
- **Legacy Vault-Key Migration** (Wave 1, Tier 2) — force/nudge pre-`PROJ-65` accounts onto the peppered scheme. Audit P0 #3.
- **Firestore Validation Extension** (Wave 1, Tier 2) — `workbook_answers`/`service`/`rosc_assessments`/`game_progress` shape/size validation.
- **Account Deletion Full-Purge Completion** (Wave 1) — `executeTotalAccountAnnihilation()` misses `service/{id}`, `rosc_assessments`, `client_errors`, and Stripe/Play-Billing subcollections. Found 2026-09-03 during the `docs/marketing/` documentation pass. Security/compliance-sensitive — spec + `zk-audit` before touching deletion logic.
- **Close Remaining Uncapped AI Cost Gap (CBT Coaching / CBA Reflection)** (Wave 1) — `cbt_coaching_prompt`/`cba_reflection` have no server-side tier check or rate limit, same shape `PROJ-106` already fixed for 3 other flows but explicitly didn't cover these two. Found 2026-09-03 during the same pass. A natural `PROJ-106`-style follow-up.
- ~~**Sub-Processor / DPA Disclosure List** (Wave 0, Tier 1)~~ **Done 2026-08-31** — see `docs/ROADMAP.md` Wave 0 for detail; also fixed a real privacy-policy gap found along the way (both `docs-site/privacy.md` and `docs/legal/PRIVACY_POLICY.md` were missing PostHog/Stripe/Google Drive from their third-party disclosures).

## ⏸️ Paused (Not in this cycle)
- **PROJ-05:** The Service Network -> Build encrypted Sponsee Rolodex to unblock organic viral growth. Paused per ROADMAP.md (Wave 3) to focus on Wave 1 Onboarding — no code exists yet beyond a Firestore rules stub; `Dashboard.tsx` still shows a "Coming Soon" placeholder. Was previously listed here as an active Priority-2 item, which didn't match its Wave-3/Paused status elsewhere — moved here to keep this file honest about what's actually being worked on.
- **PROJ-07:** Play Store TWA — **paused 2026-09-03 by user choice** (to focus on marketing materials, not blocked). TWA built/signed/uploaded to Internal Testing (release 6, v1.9.11), device-verified working; app-content checklist, Health apps declaration, and most of Store Listing/Data Safety walked through live in Play Console. **Resume checklist:** Content rating questionnaire (not started), confirm all Data Safety categories saved, the Subway Test. Full detail and a researched 2026 Play Console policy deep-dive (testing-track requirements, review timelines) in `docs/projects/07_PLAY_STORE_TWA.md` and `docs/PLAY_STORE_BUBBLEWRAP_GUIDE.md` Phase 9.
- **PROJ-105:** Google Play Billing — code complete and deployed 2026-09-02, **blocked** (not a choice-pause like PROJ-07, though on hold alongside it) on Play Console payment-method verification clearing (account-side, no ETA). A dev/emulator-only mock purchase path was added 2026-09-03 so the flow can still be tested while waiting — see `docs/projects/105_PLAY_BILLING_TWA.md`. **Resume checklist:** check Play Console Settings → Payments profile for verification status; once clear, create the `premium.monthly` subscription product and continue that spec's §6.

## 🧹 Chores & Tech Debt
- [x] **Patch `react-router-dom`/`react-router`:** Investigated 2026-08-31. `react-router`'s HIGH finding (`GHSA-qwww-vcr4-c8h2`, "RSC Mode CSRF Bypass") is a **known, already-documented accepted exception** (`docs/RUNBOOK.md`, `.github/workflows/deploy.yml`) — this app never uses RSC/framework-mode routing, and the only real fix is an untested v7→v8 major bump previously ruled out of scope. Not re-attempted here; the finalreview audit re-surfacing it was a re-discovery of an already-accepted risk, not a regression. **What was actually fixed:** a separate, newly-surfaced moderate finding — `dompurify` (transitive via `jspdf`/`posthog-js`, `GHSA-55q2-fjhq-7xh7`, XSS via detached-subtree hook removal). Added an `overrides` entry pinning `dompurify` to `^3.4.14` in `package.json` (both parents already accept `^3.3.2`, so no breaking change); `npm audit --omit=dev` now shows only the accepted react-router exception. `TD-13`. (Source: `docs/reports/2026-08_finalreview_synthesis_and_playstore_plan.md`)
- [x] **Gate `/debug` behind `isAdmin`:** `DebugTools.tsx` had only a UI warning banner, no role check (`TD-14`). Added the same `if (!isAdmin) return <Navigate to="/dashboard" />;` pattern already used by `AdminDashboard.tsx`, placed after all hooks (not before) to avoid a Rules-of-Hooks violation, and guarded `loadTasks` itself (`!isAdmin` in the early-return) so it doesn't fire a Firestore query before the redirect. Route itself intentionally left un-stripped from prod builds — kept as a genuinely useful QA tool, now actually secured instead of removed, matching the audit's own recommendation (#50).
- [x] **Align devcontainer to Node 24:** Bumped `.devcontainer/devcontainer.json`'s base image and Node feature from 20 to 24, matching CI/production (`.github/workflows/*.yml` both pin `node-version: 24`). `TD-09`. Takes effect on next container rebuild, not this running session.
- [x] **Remove vestigial `.eslintrc.json` and `vite.config.bak`:** Both confirmed unreferenced by any functional code (only self-referential mentions in this file/`ROADMAP.md`/a generated `exports/codebase_map.md`). Deleted via `git rm`; `npm run lint` re-verified clean against the real flat `eslint.config.js`. `TD-10`/`TD-11`.
- [x] **Pull and review existing `web-vitals` → PostHog data:** Code-level check only — confirmed `src/main.tsx` correctly wires `onCLS`/`onINP`/`onLCP` through `safeCapture('web_vital', ...)` to PostHog (shipped under `PROJ-94`), no gaps found, no code change needed. **Not done:** actually reviewing the resulting dashboard data requires PostHog account access outside this environment — same human-follow-up shape as the GCP budget alert item below.
- [ ] **Add Daily Crossword to the Subway Test:** PROJ-79 shipped without offline-play e2e coverage — `e2e/golden-paths/subway.spec.ts` (PROJ-73) predates the feature and doesn't exercise it. Also still missing: the "Lost PIN" test (now applicable, since solve state persists via `game_progress`). Flagged as an honest gap during PROJ-79's ticket-close, not a launch blocker — see `docs/projects/79_DAILY_CROSSWORD.md` §3/§5. **Partially closed 2026-08-03:** a general 320px-viewport (iPhone SE) screenshot check now exists (`e2e/golden-paths/viewport-320.spec.ts`, Welcome/Login/Dashboard/Tasks/VaultGate), added while closing the governance remediation plan's Phase 3 320px item for PROJ-90/96/98 — but it doesn't cover Daily Crossword itself yet, so this line item stays open for that piece.
- [x] **`AchievementsTab` journal XP "depth bonus" runs against ciphertext, not plaintext:** `calculateUserLevel`'s +10 XP bonus for journal entries over 50 words (and the unused `totalWords` stat) tests `content.trim().split(/\s+/).length` — but the journal `useQuery` this tab uses never decrypted `content`, so for any real (non-`.mock`) account it was testing a ciphertext string with no whitespace, which always split to length 1. The bonus silently never fired outside demo accounts. Pre-existing (carried over from the pre-`PROJ-76` Dashboard implementation, not a relocation regression). Found 2026-09-03 while drafting `docs/screens/profile/achievements.md`. `TD-21`. **Fixed:** the journal query now decrypts `content` (via `useEncryption()`, same context-bound pattern used elsewhere — no new Gemini/server exposure, purely a local word-count input) before it reaches `calculateUserLevel`/`calculateJournalStats`; a decrypt failure fails closed to an empty string (under-counts the bonus rather than risking an undecryptable blob scoring as a long entry). 2 new tests in `AchievementsTab.test.tsx` — one proving the bonus now actually fires on decrypted long content, one proving a decrypt failure degrades gracefully instead of crashing the tab. Caught a real type error (`tsc -b`, not `tsc --noEmit -p .`, surfaced it) during implementation: the decrypted-map's fallback branches needed `createdAt` restated explicitly to satisfy `ScorableJournal`'s required field against `DocumentData`'s index-signature typing.
- [x] **Reset Vault requires no proof of PIN knowledge:** Profile → Security's "Reset Vault" only required typing the literal word "RESET" — unlike Account Deletion's forced re-authentication step on the same page's Data tab. Anyone with access to an authenticated, unattended session could permanently destroy the vault's contents without proving anything. Found 2026-09-03 while drafting `docs/screens/profile/security.md`. `TD-22`. **Fixed:** added a `reauth` step to the Reset Vault modal, mirroring `AccountDeletionModal.tsx` exactly (Firebase Auth password or Google re-verification, same error-message mapping) — inserted between "Yes, Proceed" and the existing "type RESET" step. Deliberately Firebase Auth re-auth, not vault-PIN re-entry: the modal's own copy ("If you forgot your PIN...") advertises Reset Vault as the recovery path for someone who forgot their PIN, so gating it behind PIN re-entry would have defeated that legitimate use case — closing the "unattended session" gap without breaking the "forgot my PIN" one. 2 new tests (happy path through re-auth, and a rejected-password path proving `resetVault` is never reached), 24/24 `Profile.test.tsx` passing.
- [x] **Data tab's Import silently drops non-journal data and writes plaintext:** `importLegacyJournals` only ever read journal-shaped entries — tasks, workbook answers, and game history in an imported backup (including one this same tab's own Export just produced) were silently ignored, despite the UI copy reading as if it restores a full backup. Recovered journal entries were also written with `isEncrypted: false` and stayed plaintext in Firestore until individually reopened and re-saved. Found 2026-09-03 while drafting `docs/screens/profile/data.md`. `TD-23`. **Fixed via `docs/projects/110_DATA_IMPORT_INTEGRITY.md` (Strategy B, approved via `/planning`):** `importer.ts` (renamed `importLegacyJournals` → `importBackup`) now restores journals, tasks, and game history, re-encrypting content through the live vault key rather than writing plaintext. Workbook answers are restored as a deliberate pass-through, not re-encrypted — a second, previously undocumented bug found while investigating (`exporter.ts`'s workbook-answer decrypt logic checks a nested shape that doesn't match the real flat `workbook_answers` doc, so a real export's `answer` field was never actually decrypted in the first place) means passing it through preserves its already-encrypted state; re-encrypting it would double-encrypt genuine ciphertext. Left that export-side bug itself out of scope (Strategy B, not C) and logged as a fresh, still-open finding rather than silently bundled in — **new tech debt, not yet ledgered as its own line item; flag if touching `exporter.ts`'s workbook-answer handling.** A `zk-audit` pass during implementation caught and fixed one real risk: the workbook-answer pass-through originally trusted an incoming `isEncrypted: true` label with no shape check, which a hand-edited/malformed file could have exploited to get plaintext written under a false-encrypted label — closed with a ciphertext-shape regex guard. 8 new tests in `src/lib/__tests__/importer.test.ts` (real AES-GCM round-trips, not mocked, asserting the raw written doc is genuine ciphertext) plus 2 new tests in `DataManagement.test.tsx`; full `npm run check` clean.
- [ ] **`exporter.ts` never actually decrypts workbook answers in a Data Export:** `prepareDataForExport`'s workbook-answer decrypt branch checks for a nested `entry.answers` map (`{[questionId]: {isEncrypted, text}}`) that doesn't match the real `users/{uid}/workbook_answers` doc shape (flat: `{workbookId, sectionId, questionId, answer, isEncrypted, updatedAt}` — one doc per question, confirmed via `src/lib/workbookAnswers.ts`/`fetchAllUserData`). The branch never matches, so unlike journals and game-progress (which do decrypt correctly), a real export's workbook `answer` field ships as raw ciphertext — silently, no error. Found 2026-09-03 while scoping `docs/projects/110_DATA_IMPORT_INTEGRITY.md` (PROJ-110); deliberately left unfixed there (Strategy B, not C — see that spec's Phase 1) since PROJ-110's import-side fix was designed to work correctly either way. Practical effect: a downloaded "backup" JSON's workbook answers aren't human-readable even though the export screen implies a full plaintext backup, and they can only ever be restored on the same device/vault-key that exported them (an export from before a vault reset or PIN rotation can no longer decrypt on import). `TD-26`.
- [x] **Data tab's Google Drive auto-backup fails completely silently:** `AppShell.tsx`'s `performAutoBackup` caught every *thrown* failure into a bare `console.error` — nothing surfaced in the UI. Found 2026-09-03 while drafting `docs/screens/profile/data.md`. `TD-24`. **Investigation found it was worse than described:** (1) `uploadBackupToDrive()` returns `false` (not a thrown error) on an HTTP failure like a revoked Drive permission or hitting quota — that path hit neither the catch block nor even `console.error`, doing nothing at all; (2) Dashboard's existing "Backup Needed" banner is gated on `!driveAccessToken`, so a Drive-*connected* user whose auto-backup kept silently failing would never see it — "eventually reappearing days later" was inaccurate; it could stay silent indefinitely. **Fixed:** both failure paths now write `users/{uid}.lastAutoBackupFailedAt` (new plaintext metadata field, `db.ts`; cleared on the next successful run); the Dashboard banner's condition now also fires for a Drive-connected user with that field set, with distinct copy ("Your last Google Drive backup didn't go through") from the no-Drive-connected nudge. 3 new Dashboard tests covering all three states (no-Drive nudge / Drive-connected-and-healthy silence / Drive-connected-and-failed). No new test infra added for `AppShell.tsx` itself (no prior test file existed; the change there is a small, low-complexity if/else + catch addition) — flagged as a scope call, not silently skipped.
- [ ] **Tasks' "Smart Reset" grace-window mechanic is dead code:** `getUserTasks()` (`src/lib/tasks.ts`) contains a real, correct, unit-tested implementation of the documented behavior (overdue recurring tasks silently rolled forward with a 2-hour grace window and a streak penalty) — but nothing in the live app calls it. `Tasks.tsx` renders from `useTasksList()`'s plain `onSnapshot` listener instead, confirmed by that hook's own code and independently by `useTaskOperations.ts`'s header comment. Practical effect: an overdue recurring task just sits overdue indefinitely until manually touched — `missedCountHistory` never accumulates from this path. The existing, tested function is presumably wirable back in without a rewrite; still worth scoping deliberately (where should it run — on load, in a Cloud Function, elsewhere?) rather than patched ad hoc. Found 2026-09-03 while drafting `docs/marketing/tasks.md`; docs/screens/tasks/ corrected same day. `TD-25`.
- [ ] **Tighten `tsconfig.app.json`:** add `noUncheckedIndexedAccess` (Deferred: Evaluated in Sprint 9.0 and found 164 compiler errors; needs dedicated refactoring cycle). Separately evaluate `exactOptionalPropertyTypes` — may surface a batch of new type errors to clean up first. (Source: §6)
- [x] **Document `syncStripeSubscription` trust dependency:** Added a security warning comment to `syncStripeSubscription` in `functions/src/index.ts` explaining the firestore.rules trust dependency boundary. (Source: §4)
- [ ] **Converge the three "admin" definitions:** `AuthContext.tsx:50` hardcodes `currentUser.email === 'rpdouglas@gmail.com'` as a client-side admin-UI bypass alongside the real custom claim and the `role` field — no privilege-escalation risk (every real admin read/write still requires the custom claim), but three informal definitions of "admin" should converge on one. (Source: §4)
- [x] **Align Cloud Functions lint tooling:** Aligned ESLint to `^9.39.2`, TypeScript to `~5.9.3`, and `@typescript-eslint/*` to `^8.50.0` inside `functions/package.json`. Migrated functions to flat config `eslint.config.js` and removed legacy `.eslintrc.js`. (Source: §7)
- [x] **Add bundle visualizer:** Installed `rollup-plugin-visualizer` (dev-only) and configured it in `vite.config.ts` to output size stats to `stats.html`.
- [x] **Add dead-code tooling:** Installed `knip` dev-only and configured `knip.json`. Executed a live dead-code sweep, deleting 10 unused files and 5 unused package dependencies (removing 109 packages from the node tree). (Source: §6, §7)
- [x] **Document `RecoveryCapital.tsx`'s exhaustive-deps suppression:** Line 56's mount-only effect (`?start=1` deep-link handler) intentionally uses `[]` deps — confirmed correct (not a bug) since the route is wrapped in `<VaultGate>`, so the vault-locked branch of `ctaDisabled` can't be reached at mount, and re-running on later state changes would risk re-opening the modal after dismissal. Added a comment explaining the reasoning rather than changing the deps array. (Source: debt-ledger sweep 2026-08-04)
- [x] **Document `GuidedWorkflowEngine.tsx`'s 3 exhaustive-deps suppressions:** Lines 111, 148, 166 — confirmed all three are correct as written (avoiding a re-shown resume prompt, avoiding clobbering user edits on every keystroke, and avoiding an unmemoized `getAiContext` prop resetting the AI debounce timer on every parent re-render). Added explanatory comments to each rather than changing the deps arrays. Lint clean, 20/20 existing unit tests still pass. (Source: debt-ledger sweep 2026-08-04)
- [x] **Fresh `knip` findings beyond PROJ-97's scope:** triaged all 41 (16 unused exports + 25 unused exported types) individually — none taken at face value. `GROUP_ORDER` (`journalTemplates.ts`) was genuine dead code left behind by PROJ-93's clinical-group→moment-based regrouping; deleted, along with 7 other genuinely-dead types (`AnalysisResult`/`AIAnalysisResult` in `gemini.ts`, `KnowledgeQuestStats`/`ThoughtChallengeStats`/`TriggerMatchStats` — each superseded by an inline anonymous type in its own game component, `AssetPath`, `InsightType`, `Frequency`). The 15 `mockData.ts` per-persona constants plus 13 more types were used internally all along (same over-exported pattern PROJ-97 fixed) — `export` dropped, code kept. 4 — `SmartToolMetadata`/`SelfCompassionPayload`/`SmartGoalPayload`/`BoundariesPayload` — turned out to be deliberate scaffolding for 3 SMART Recovery tools already registered in `toolsRegistry.ts` and explicitly documented as "no shipped UI yet" (`toolHistorySummary.ts:37`); left untouched. `npm run check` clean throughout (lint, 68 specs, 701/701 tests, build). (Source: PROJ-97 `/planning` pass, 2026-08-04)

## ✅ Resolved This Cycle
- [x] **PROJ-97:** Dependency Hygiene Round 2 — Replaced `@use-gesture/react` with native touch handlers in `SwipeableTaskRow.tsx` (measured 20KB raw / 6.5KB gzip off the vendor chunk) plus a new 8-test suite that didn't exist before; deleted verified-dead `knip` findings and dropped the `export` keyword from 4 more that turned out to be used internally, not dead; added `eslint-plugin-jsx-a11y` and triaged all 48 findings it surfaced (fixed ~40, suppressed the rest with documented reasoning). `crossword-layout-generator` replacement descoped to a trigger-based `BACKLOG.md` item.
- [x] **PROJ-101:** Data Access Layer Consolidation — Round 2 — Data Access Layer Consolidation Round 2 — finished the useFirestoreCrud migration PROJ-59 started: migrated useJournalOperations/useWorkbookAnswers/useTaskOperations onto its shared query/mutation primitives (extending the mutation primitive with an optional onSuccess hook along the way), removed useTaskOperations' dead optimistic-update machinery after finding it targeted a cache key the real Tasks page never reads, and added the first direct AuthContext unit tests (isAdmin claim/role-fallback logic, the DEV-only mockUser bypass, onAuthStateChanged's null-user and profile-fetch-failure paths).
- [x] **PROJ-100:** AI Prompt Safety Hardening — AI Prompt Safety Hardening — closed the AI-safety gaps in generateAIInsights: real payload schema validation replacing presence-only checks and blind as-casts (also closing a latent unvalidated-scope read in the rate-limit branch), prompt-injection delimiting across all nine analysisType flows, and dual-profile Gemini safetySettings (permissive for the nine user-content flows given this app's crisis-journaling subject matter, stricter for the two editorial content-generation calls).
- [x] **PROJ-99:** Firestore & Backend Hardening — Closed the Firestore/Cloud-Functions-layer findings from the production-readiness audit: added document-shape/size validation to firestore.rules (journals/game_saves), the repo's first Firestore rules unit test suite (22 tests via a real emulator), 2 missing composite indexes, Cloud Functions cost guardrails (maxInstances on generateAIInsights/dailyBeacon) plus a real dailyBeacon sendEach 500-message correctness fix, and a safe-migration telemetry signal ahead of eventually converging the admin-definition fallback.
- [x] **PROJ-98:** Audit Quick-Win Remediation Sweep — Closed 10 quick-win findings from the production-readiness audit: PWA install size cut ~82%, 15 accessibility labels added, 3 previously-undocumented AI call sites reviewed and approved into the governance list, functions/ dependency vulnerabilities reduced, and context re-render performance hardened.
- [x] **PROJ-96:** CI/Deploy Operational Safety Net — Closed the CI/deploy operational safety net gap: added hard-fail production-dependency vulnerability gates to the verify job (root + functions/ workspaces, each scoped to its own lockfile), remediated 2 critical + 4 high vulnerabilities in functions/ that no prior audit had ever covered, and wrote docs/RUNBOOK.md covering Hosting/Functions/rules rollback procedures and the gate's one documented exception (a non-applicable react-router RSC-mode CVE).
- [x] **PROJ-95:** Journal Read-Cost Reduction (Dashboard Anchor Status + Journal History) — Fixed useAnchorStatus.ts (used by Dashboard on every app open) doing an unbounded full-history journals read just to check if any entry exists from today - scoped to a date-range query with its own distinct cache key (critical: it previously shared a key with JournalInsights.tsx/AchievementsTab.tsx, which genuinely need full history). Restructured JournalHistory.tsx from a single unbounded fetch to two modes - current-year-only by default, explicit opt-in full-history for the AI Wizard/search-everything/browsing prior years - after finding the originally-planned per-year incremental loading had a real discoverability flaw. Updated the user guide to match.
- [x] **PROJ-94:** Production Error Telemetry Hardening — Fixed ErrorBoundary's guaranteed-to-fail Firestore write when unauthenticated (PostHog capture already covered this case). Added trackMutationFailed telemetry to the real leverage points: the shared useFirestoreMutation wrapper's onError (covers 3 files), all 4 of useTaskOperations' hand-rolled mutations (previously zero visibility of any kind on a failed task write), and useJournalOperations' 3 mutations (previously duplicated ad-hoc across 3 calling components). Added trackClientError for 8 genuinely scattered console.error sites in AuthContext/EncryptionContext. Added web-vitals reporting. All new telemetry call sites verified to pass only domain + error name, never decrypted content.
- [x] **PROJ-93:** Journal Template Moment-Based Grouping — Regrouped the journal template picker from clinical-modality groups to moment-based sections (In the Moment / Daily Rituals / Reflection & Insight), rendered as a collapsible bottom-sheet accordion; each template still shows its modality as secondary metadata.
- [x] **PROJ-92:** Performance & Scale Polish — Added explicit gcTime (30 min) to the QueryClient defaults, previously unset. Removed lucide-react entirely, consolidating onto @heroicons/react across 4 files (the dependency audit that flagged this missed one of the 4 files and undercounted FeedbackViewer.tsx's icons by 3). Verified the rest of the originally-planned scope (list virtualization, PWA precache, Firestore read/index audits, asset sizes) was already fine on inspection — documented rather than touched.
- [x] **PROJ-91:** WCAG 2.2 AA Accessibility Remediation — Fixed 4 real accessibility violations found by direct code review (icon-only buttons missing aria-labels, a keyboard-inaccessible popover) plus 3 more found only by a new automated axe-core scan (disabled pinch-zoom app-wide, an unlabeled Dashboard menu button, a contrast failure on the Backup Needed banner) — disproving the audit's original theme.ts contrast hypothesis in the process. Added e2e/golden-paths/a11y.spec.ts as a permanent CI regression gate.
- [x] **PROJ-90:** Security, Dependency & Compliance Hardening — Ran npm audit fix (non-force) to clear the ws/postcss/react-router-dom vulnerabilities, removed 8 stray console.log statements and added an esbuild.pure guard against future ones, added a Play Store health-app medical disclaimer to Login and Welcome, and added a production-build e2e regression test proving the mockUser bypass stays fixed.
- [x] **PROJ-89:** Play Store Release Blockers — Fixed the two hard blockers from the Production Readiness Audit (`docs/reports/archive/PRODUCTION_READINESS_AUDIT.md`, 2026-07-28): gated the `mockUser` URL-param client-side auth bypass behind `import.meta.env.DEV` in `AuthContext.tsx` (SEC-01 — the screenshot pipeline that relies on it, `scripts/generate_screenshots.js`, only ever drives the Vite dev server, so it's unaffected), and split the 1.83MB monolithic `vendor` bundle in `vite.config.ts` into `react-vendor`/`react-router`/`tanstack-query`/`icons`/`pdf-export`/`posthog` buckets (PERF-01 — final `vendor` chunk: 867KB, no chunk over 1MB). A first-pass regex for `react-vendor` was too broad and caught `@use-gesture/react`'s subpath, producing a `vendor <-> react-vendor` circular-chunk warning; tightened to match only the literal top-level `react`/`react-dom`/`scheduler` packages, which resolved it. Full `npm run check` clean, 662/662 tests. Per the audit's own verdict, this was the last blocker to Google Play submission readiness. See `docs/projects/89_PLAY_STORE_RELEASE_BLOCKERS.md`.
- [x] **PROJ-87:** Trigger Match & Knowledge Quests Immersive Shell Restyle — Restyled Trigger Match and Knowledge Quests to match the dark-immersive shell used by Goal Ladder/Jeopardy, via an opt-in theme prop on the shared quiz component so the still-light Thought Challenge is unaffected.
- [x] **PROJ-88:** Daily Crossword Immersive Shell Restyle — Restyled Daily Crossword to match the dark-immersive shell used by Goal Ladder/Jeopardy/Trigger Match/Knowledge Quests, and fixed a mobile on-screen-keyboard bug where typed letters weren't reaching the grid.
- [x] **PROJ-86:** Recovery Jeopardy Immersive Shell Restyle — Extended the Goal Ladder-style dark-immersive shell (background, header score chip, footer) to Recovery Jeopardy across every round, replacing GameShell's light chrome for this game.
- [x] **PROJ-85:** Goal Ladder Immersive Redesign & Hero-Derived Dashboard Theming — Rebuilt Goal Ladder as a self-contained dark-immersive screen with a literal SVG rung-climb visual, and made Dashboard's header/background derive from the user's chosen Sobriety Hero color.
- [x] **PROJ-84:** Dashboard Bento Tile Refresh & Tasks Contrast Fix — Componentized the six dashboard tiles into a THEME-driven BentoCard, and darkened THEME.tasks.header one shade for white-text legibility.
- [x] **PROJ-83:** Sobriety Hero Typography Scale-Up — Scaled up Years/Months/Days, Total Days, and Saved text on the Dashboard sobriety hero by one Tailwind type step.
- [x] **PROJ-82:** PostHog Telemetry Expansion & Zero-Knowledge Alignment — PostHog Telemetry Expansion — safe, ZK-guarded analytics across crisis modal, urge surfer, recovery games, somatic vitality, and React error boundary.
- [x] **PROJ-81:** Games Hub Design System Alignment — Corrected PROJ-80 drift by replacing GamesHub's one-off back-button header with the shared VibrantHeader component and retinting its dark hero card to match the Dashboard's "My Games" bento tile, restoring the always-available SOS button.
- [x] **PROJ-80:** Games Hub Unified Hero Restyle — Restyled the Recovery Games hub into one unified dark card with a flat game list (replacing the tile grid), delisted Craving Buster/Thought Challenge from it without touching their routes or components, and surfaced Thought Challenge as a new Tools Hub entry so it isn't left unreachable.
- [x] **Maintenance:** Docs Archive Cleanup — reviewed `docs/projects/` and `docs/reports/` for bloat before it compounded further. Restored 3 specs (`24_ASSET_ENGINE.md`, `39_DEFERRED_VAULT_LOCK.md`, `48_USER_GUIDE_SYNC.md`) from `docs/projects/archive/` to the main tree — they had no genuine "superseded by" relationship and were inconsistent with this repo's own "shipped stays in place" convention (only 2 of the 5 archived specs actually had a pointer note). Archived 6 `docs/reports/` files whose findings are now fully absorbed into shipped specs or superseded by newer reports (`codebase_gaps_audit_report.md` + `remediation_implementation_plan.md` → PROJ-64/65, `google_play_readiness_report.md` → superseded by `2026-07_app_readiness_review.md`, `profile-gap-analysis.md` → PROJ-58, `july_2026_features_and_code_review.md` and `2026-07_recovery_games_platform_spec.md` → superseded by the current ledger / PROJ-72's canonical spec), updated every citing doc's path reference to match, deleted a stale `.html` duplicate of `profile-gap-analysis.md`, and renamed/archived the download-artifact-named `SPEC-crossword-001 (1).md`. Confirmed `DailyCrossword.jsx`/`DailyCrosswordClassic.jsx` are *not* misplaced — they're design-reference mockups actively cited by path from `DailyCrossword.tsx` and its tests, now documented as a deliberate exception in a new `docs/reports/README.md`. Added `governance` skill Check J (flag-only, same posture as the rest of that skill) to catch this drift going forward instead of relying on another ad hoc sweep.
- [x] **PROJ-79:** Daily Crossword — Added Daily Crossword, the 8th Recovery Games entry — a server-generated, non-personalized daily puzzle persisted like every other game but deliberately excluded from XP, matching its reward-free design.
- [x] **Maintenance:** Build-Process Tooling Gap Closure — added 5 new Claude Code skills (`deps-audit`, `ingest`, `debt-ledger`, `release-scribe`, `secrets-scan`) and a `PreToolUse` git-commit hook that blocks commits containing likely secrets (private keys, AWS/Google/Stripe/GitHub/Slack token shapes, hardcoded credential literals). Closes gaps found in a full audit of `.claude/` tooling against the Recursive Build Protocol and Maintenance Protocols B/C, plus external research into comparable Claude Code skill ecosystems. `npm run check` clean.
- [x] **PROJ-78:** VibrantHeader Right-Column Overflow — VibrantHeader Right-Column Overflow — fixed a layout bug where the header's shrink-0 center title/subtitle column never yielded width, pushing the SOS button up to 29px past the viewport edge on pages with a long subtitle (worst case: Workbooks at 412px/320px). Fix: center column now min-w-0 with a truncating subtitle; SOS's own column deliberately untouched so it can never shrink.
- [x] **PROJ-77:** Nav Tab-Bar Unification — Nav Tab-Bar Unification — extracted a shared TabBar component and migrated Journal, Vitality, Workbooks, Tasks, and Profile onto it, sourcing per-feature colors from theme.ts. Fixed a real overflow/overlap bug at narrow viewports found during QA (min-w-fit + overflow-x-auto).
- [x] **PROJ-76:** Gamification Dashboard Relocation — Moved the Rank/Level/XP display and every bento tile's stat numbers off the Dashboard into a new "Achievements" tab on Profile, so the Dashboard reads as a calm core-recovery snapshot rather than a gamified one — no gamification logic deleted, just relocated.
- [x] **PROJ-75:** Workbook Marketplace (v1 — Official Catalog) — Added workbook marketplace v1 — Marketplace tab lets users add/remove official workbooks from their library via a new installedWorkbookIds profile field, no new Firestore collection or rules.
- [x] **PROJ-73:** Test Suite Hardening — Vault-PIN Pepper Coverage. Closes the coverage gap left by PROJ-65: e2e Vault Test now exercises the real peppered derivation path (Functions emulator added to `test:e2e`), direct unit coverage of `EncryptionContext.tsx` added (12 tests), and `verifyVaultPin`'s decision logic (`evaluateVaultPinAttempt`/`deriveVaultPepper`) extracted and unit tested. Phase 4 (stretch — Recovery Games): component-test coverage shipped 2026-07-22 (15 new tests across the 6 remaining games); Subway Test (PROJ-72's browser QA gap) automated 2026-07-23 as a new `e2e/golden-paths/subway.spec.ts`, run for real against local emulators — closing this project's remaining scope. **The actual regression check performed for real** (two rounds against the live `verifyVaultPin` pepper derivation, confirmed `vault.spec.ts` genuinely depends on it rather than silently passing via a legacy fallback). See `docs/projects/73_TEST_SUITE_HARDENING.md`.
- [x] **PROJ-74:** Legacy Vault Unlock Hang on Decrypt-Mismatch — Legacy Vault Unlock Hang — a decrypt mismatch during legacy (pre-pinVerifier) vault unlock now fails closed with a clear error instead of hanging the unlock button indefinitely.
- [x] **PROJ-72:** Recovery Games — all 7 phases shipped (Sprints 9.2–9.8, PRs #114/#115/#117/#118/#119/#120/#121). Foundation (encrypted `game_progress`, typed `IRecoveryGame` SDK) → Morning Intent + Craving Buster (David) → Recovery Jeopardy (group activity, ported from a legacy sibling project with a full trademark scrub) → Fast Lane (Walt, a multi-week economic life-sim, introducing a fully-encrypted `game_saves` collection for multi-session state) → Goal Ladder (Ned) / Thought Challenge (Lisa) / Trigger Match (Walt), sharing a common quiz loop → Knowledge Quests (persona-agnostic psychoeducation packs, a new `'All'` `GamePersonaTarget` value) → a closing Data & Sharing pass that found PROJ-30's export/deletion engine already existed and extended it to cover Recovery Games data, fixing a real gap along the way (account deletion never scanned `game_progress`/`game_saves`), plus added Milestone Image sharing to Fast Lane/Jeopardy win screens. Full `npm run check` clean throughout. Subway Test (offline play/resume, Fast Lane's close-tab-and-resume through `game_saves`) automated 2026-07-23 under PROJ-73 as `e2e/golden-paths/subway.spec.ts`, run for real against local emulators. See `docs/projects/72_RECOVERY_GAMES.md`.
- [x] **Maintenance:** SMART Tool Journal Entries — Journal History previously showed completed SMART Tool sessions (CBA, ABC, D.E.N.T.S., Personify, Lifestyle Balance, Thought Record, Five Questions) as raw JSON text instead of readable content. Extracted the per-tool History page's existing parsing/rendering pattern (`parseSmartToolPayload`, `PayloadSummaryList`) into a shared helper and reused it in the main Journal History timeline: entries now show a tool badge, a one-line headline, and an expandable humanized field list. Also fixed search and Share to use humanized text instead of raw JSON, hid unfinished drafts and the Edit button (editing raw JSON in the freeform editor would corrupt it), and humanized tool entries before they're sent to Gemini in both AI analysis flows (`JournalAnalysisWizard`, `useDeepPatternAnalysis`).
- [x] **PROJ-71:** Tools Hub Regrouping — Regrouped the flat "My Tools" list into four moment-based collapsible accordion sections (Right Now / Before It Happens / After a Hard Moment / Big Picture), with Right Now expanded by default so David's crisis tools (Urge Surfer, Resentment Burner) are always immediately visible. Added a "Continue where you left off" resume callout and brought the page onto the Momentum Kinetic visual system via a new `tools` `GlassCard` variant (amber → orange). Caught and fixed a "Best for" badge overflow on narrow viewports during visual QA. See `docs/projects/71_TOOLS_HUB_REGROUPING.md`.
- [x] **PROJ-23:** The QA Sentinel — Playwright E2E golden-path suite (Gate/Vault/Ledger) wired into CI as a blocking pre-deploy gate against local Firebase emulators. Found and fixed a real journal-entry data-loss race condition along the way, plus three CI-only environment gaps (firebase-tools, Java 21, Firebase client env vars) invisible in local dev.
- [x] **PROJ-70:** Changelog Historical Scrub — Rewrote all remaining changelog entries (v1.0.0-v1.8.18) into plain user-facing language: stripped PROJ-ID tags, file/hook/component names, and engineering jargon; deleted or trimmed entries with zero user-facing content (admin-only tooling, pure test/architecture work).
- [x] **PROJ-69:** Changelog Split — Public/Internal Separation — Split the public docs-site changelog from internal engineering detail: scrubbed two live security disclosures and rewrote five mislabeled entries, added a leak-guarded --public-note/--version path to sync_ticket_docs.py, and added a Check 0 user-visible classification gate to the ticket-close skill.
- [x] **PROJ-68:** Gate Stripe Checkout Out of the Android TWA — closes out the Stripe/Play Billing policy research item (2026-07-19 research found the question genuinely jurisdiction-dependent and time-boxed to a US-only antitrust remedy expiring 2027-11-01). Decided to sidestep the legal ambiguity entirely rather than resolve it: gate the in-app Stripe purchase flow out of the Play-installed TWA instead of pursuing full Google Play Billing integration. Added `isAndroidTWA()` (`src/lib/platform.ts`, detects the `android-app://` referrer specific to a TWA launch — deliberately narrower than the existing `display-mode: standalone` check, which also matches desktop/iOS PWA installs outside Play's policy scope). `PremiumUpgrade.tsx`'s "Become a Supporter" purchase button is replaced with an "Upgrade on the Web" link in the TWA; "Manage Subscription" for existing subscribers remains ungated (account management of an existing subscription, not a new external purchase). Unblocks PROJ-07 Sprint 9.2 — the last remaining blocker was this policy question. See `docs/projects/68_STRIPE_TWA_GATING.md`.
- [x] **npm audit fix:** 7 production vulnerabilities found in a clean install (2 critical, 3 high, 2 moderate), including `react-router-dom` in the vulnerable 7.0.0–7.15.0 range. Found in `docs/reports/archive/2026-07_app_readiness_review.md` §1 (2026-07-18). Fixed 2026-07-19 via `npm audit fix` — resolved to 0 production vulnerabilities, no `package.json` bump needed (lockfile-only, stayed within the existing `^7.10.1` semver range). Full `npm run check` re-verified clean after the fix (66/66 test files, 453/453 tests, lint clean, build succeeded). 13 dev-dependency vulnerabilities remain, intentionally deferred as lower-priority (don't ship to users).
- [x] **PROJ-65:** Vault PIN Brute-Force Hardening — a 2026-07-13 commit's message claimed the PIN key-derivation scheme was hardened against brute-force; it wasn't (only telemetry landed, `crypto.ts`'s key derivation is untouched). `/planning` session (2026-07-17) resolved the multi-device objection with a rate-limited server-pepper design (not the originally-proposed IndexedDB Master Key); implemented same day across `functions/src/index.ts`, `crypto.ts`, `rotation.ts`, `EncryptionContext.tsx`, `firestore.rules`. A `zk-audit` pass caught one bug via code review (rotation's new-PIN pepper fetch didn't recognize `pendingRotation.verifier`). Manual end-to-end verification against real local Firebase emulators then caught a second, more serious bug code review missed — a Firestore transaction that threw to reject a wrong PIN was also discarding its own attempt-counter write, making the rate limiter a silent no-op. Fixed and re-verified: Subway Test and Lost-PIN Test both pass for real. **External security review passed 2026-07-19** — see `docs/projects/65_VAULT_KEY_HARDENING.md` §5.
- [x] **PROJ-67:** Signing Key & Secrets Hygiene — `mrt-release.keystore` was tracked in git (`.gitignore` entry added too late to have effect). Found in `docs/reports/archive/2026-07_app_readiness_review.md` §1 (2026-07-18). New keystore generated and stored in Google Secret Manager (`mrt2-app-prod`); `assetlinks.json` fingerprint updated; old keystore purged from all git history via `git filter-repo` and force-pushed to `origin/main` (2026-07-19), verified zero remaining references both locally and on the remote. Full mirror-clone backup retained as a rollback safety net. Unblocks PROJ-07 Sprint 9.2. See `docs/projects/67_SIGNING_KEY_SECRETS_HYGIENE.md`.
- [x] **PROJ-64:** Gemini AI Proxy & Platform Hardening — Cloud Functions proxy (`generateAIInsights`) removes the client-exposed Gemini API key and moves AI rate-limit enforcement server-side; Firestore now boots with a persistent multi-tab `IndexedDB` cache for offline resilience; `crypto.ts`'s `decrypt()` now reports HMAC/decryption failures to PostHog. Shipped 2026-07-13 (commit `6748388`); spec backfilled 2026-07-16 during a governance audit that also found the shipping commit's message overstated its scope — see `PROJ-65`.
- [⛔ Abandoned] **React 19 Refactor:** Incremental migration to `useActionState` evaluated and abandoned due to poor ROI on purely client-side SPA forms. See [react19_action_state_assessment.md](file:///home/node/.gemini/antigravity-cli/brain/7fdfa35e-619b-4a30-852b-93e6ba4e406b/react19_action_state_assessment.md).
- [x] **PROJ-63:** Mobile Screenshot Generator — Implement automated mobile screenshot generator with persona-based mock bypass.
- [x] **Report archived:** `docs/reports/archive/2026-07_codebase_deep_review.md` → `docs/reports/archive/`. Verified against current code: 26 of 30 findings fixed (Firestore-bypass consolidation, dead-code removal, god-file splits, `any` suppressions, Firestore rules, CI heredoc, debug log, test-coverage backfill). Remaining 5 open items carried forward below (`noUncheckedIndexedAccess`, `syncStripeSubscription` trust-dependency comment, hardcoded admin-email convergence, Cloud Functions lint alignment, bundle/dead-code tooling).
- [x] **PROJ-62:** Tech Debt Quick Wins — Logging, Rules, Duplicate Reads, `any` Suppressions — Cleared five deep-review quick-win chores: removed a debug console.log, hardened the CI service-account heredoc, tightened ai_logs/client_errors/feedback Firestore rules to uid-match, extracted Tasks.tsx's raw onSnapshot into useTasksList, and fixed the last four any-type suppressions (two of which were masking real latent bugs).
- [x] **Maintenance:** Journal Sharing — Appended a blank line followed by `myrecoverytoolkit.ca` to the end of the shared journal text, added screen reader accessibility title to the Share button, and backfilled corresponding unit tests.
- [x] **PROJ-61:** Test Coverage Backfill — ZK-Adjacent & Firestore-Write Paths — Backfilled test coverage for exporter.ts, useROSCAssessments, useRateLimits, and functions/prompts.ts prompt-construction logic.
- [x] **PROJ-60:** God File Decomposition — Vitality & Data Management — Split Vitality.tsx and DataManagement.tsx along independent concerns, isolating account deletion and fixing a ZK plaintext-write bug in Vitality journal saves.
- [x] **PROJ-59:** Data Access Layer Consolidation — deleted two dead journal-CRUD paths (`lib/journal.ts` plus two unused `db.ts` exports, zero importers confirmed); fixed a cache-invalidation bug where `useJournalOperations` invalidated an orphaned `['journals']` key while every reader queried `['journals', uid]`, silently breaking Dashboard's journal-streak refresh; consolidated 8 duplicate `users/{uid}` profile-fetches onto the existing `useUserProfile()` hook; extracted `useFirestoreQuery`/`useFirestoreMutation` primitives (`useFirestoreCrud.ts`) and migrated every raw Firestore call site flagged in the spec onto them, reusing existing lib functions (`getUserTemplates`, `saveInsight`) instead of duplicating them where they already existed. Also fixed two bugs surfaced by the new test coverage: `JournalEditor`'s smart-mood prefill was reading the same orphaned cache key (never worked in production) and `DynamicAnchorWidget` had a `FELLOWSHIPS.DEFAULT` reference that doesn't exist, crashing for any user without a saved fellowship preference when their daily-readings collection was empty.
- [x] **PROJ-58:** Profile UX Remediation — unified three incompatible save models (explicit submit, busy-flag, silent optimistic) into one autosave pattern with a shared "Saving… / Saved" indicator; fixed a TanStack Query architecture bypass in `Profile.tsx` that could leave Dashboard badges stale after a save; restyled the vault-reset confirmation from a native `prompt()`/`alert()` into the same Headless UI dialog pattern used for account deletion; made Security/Data settings deep-linkable routes (`/profile/security`, `/profile/data`); replaced a generic "check console" import-failure message with cause-specific ones; surfaced a distinct partial-failure state when the Firestore save succeeds but the Firebase Auth displayName sync fails; and closed a handful of polish gaps (sub-44px swatch touch targets, a duplicated vault-unlocked banner, unbounded sobriety-date input).
- [x] **PROJ-57:** Journal Template Modality Expansion — expanded the default journal template picker from 4 Twelve-Step-only templates to 15 across 11 recovery modalities (CBT/SMART, DBT, Mindfulness, Harm Reduction, Reset, Trauma-Informed, ACT, Motivational, MAT, General), grouped by modality in the picker. New templates reuse the existing guided multi-prompt form/save path already used for custom templates — no new rendering logic.
- [x] **PROJ-56:** Sobriety Hero Color Themes — swatch button on the dashboard hero (top-left) plus a matching "Hero Appearance" picker in Profile → General. 5 curated presets, stored as a plaintext `heroColor` field on `users/{uid}`, defaulting to today's amber look when unset.
- [x] **PROJ-55:** Workbook Remediation — Rewrote Steps 2-11 of the 12-Step workbook with real, step-specific questions (15 unique literature-grounded contexts each), replacing a templated placeholder. Migrated `WorkbookDetail`/`WorkbookSession`/`useAutoSave` off direct Firestore calls onto a new `useWorkbookAnswers` TanStack Query hook, fixing a fragile string-based decryption heuristic in the process. Deleted an orphaned duplicate data module and made the gamification workbook-completion denominator compute dynamically instead of a stale hardcoded value.
- [x] **PROJ-50:** Guided CBT/REBT Interactive Workflows — Transformed the CBT tool suite from open-form static inputs into guided, step-by-step interactive flows (`GuidedWorkflowEngine`) with contextual coaching, optional AI-assisted prompts, and psychoeducation. Shipped in 6 phases: foundation + guided ABCDE, guided CBA, a brand-new 7-column Thought Record, DENTS Scenario Mode + a brand-new Five Questions self-enquiry tool, and a Tools Hub redesign (Start Fresh / Resume / History entry points, completion tracking, time estimates).
- [x] **PROJ-54:** Journal Insights Momentum UI Redesign — Refactored the Insights tab into an atmospheric, reflection-first "Glass and Glow" interface utilizing Momentum Kinetic v3.0, replacing dry charts with semantic colors and ambient motion.
- [x] **PROJ-53:** ROSC Matrix Visual Upgrade (Pill Capsules) — Replaced Recharts radar chart with custom animated "Pill Capsules" design featuring premium glassmorphic dark theme and longitudinal ghosting.
- [x] **PROJ-49:** The Recovery Capital (ROSC) Matrix — Self-reported check-in + Gemini AI analysis across SAMHSA's four dimensions (Health, Home, Purpose, Community), monthly for free tier / weekly for premium. Full-screen `/insights/rosc` route (Snapshot/Trends/History tabs) with real Recharts longitudinal trend charts, superseding the original single-assessment "Pill Capsules" ghosting. Per-domain suggested actions with one-tap "Add to Tasks." ZK boundary: domain scores unencrypted, AI narrative/actions encrypted. Premium AI insights; free tier self-report scores.
- [x] **PROJ-47:** The Ledger — Precision, Resilience & Tab Redesign. Monthly day-drift fix (`originalDayOfMonth`), 2-hour midnight grace window, `missedCountHistory` append, Today/Later/Log tab redesign (Action Plan tab removed), "From yesterday" overdue label.
- [x] **PROJ-48:** User Guide Synchronization Sprint — new Daily Readings page (09), Dynamic Anchor rewrite (2-button bar, time-of-day table, notify toggles), Voice-to-Vault full section, recurrence schedule table, Smart Reset / Lazy Evaluation expansion.
- [x] **PROJ-46:** The Ledger — Frictionless Task Module Upgrade. Sprint 1: swipe-to-complete (green reveal + haptic), Forgiveness Tap (amber swipe-left, "Let today go" sheet), Pull-to-Add Quick Capture. Sprint 2: Rhythm Score (14-day ring), AI Context Cards (sourceContext + sourceRef deep-links), Gemini prompt updates for action_contexts.
- [x] **PROJ-41:** Dynamic Anchor -> Replaced static 'Daily Pledge' with a circadian-aware, 3-column quick action bar on the dashboard.
- [x] **PROJ-40:** Test Suite Audit -> Comprehensive review of Vitest pipeline.
- [x] **PROJ-31:** Crypto Chunking Pipeline -> Refactor PIN rotation to handle 10k+ docs without UI thread locking.
- [x] **PROJ-39:** Deferred Vault Lock -> Added 'Skip for Now' onboarding bypass.
*(Queue Empty)*
*(Queue Empty)*
