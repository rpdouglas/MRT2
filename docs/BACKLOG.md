# 🧊 Feature Backlog (The Persona Icebox)

**Last Reviewed:** 2026-08-31

**Storage:** Ideas and feature requests that are approved but deferred. Tagged by Persona to ensure we are building for specific psychological needs, not just adding features.

## 👤 David (The User in Crisis)
* **Feature:** Harm Reduction Mode.
  * **Concept:** A toggle that shifts the app's language from "Abstinence" to "Management" (e.g., tracking drinks per week instead of days since last drink).
  * **Status:** Deferred to post-Wave 2.
* **Feature:** Clinical Telehealth Off-Ramps (MAT Resources).
  * **Concept:** Direct links to Medication-Assisted Treatment if the SOS button is pressed multiple times.
  * **Status:** Deferred — unscheduled.
* ~~**Feature:** Bundle Size / Code-Splitting Reduction.~~ **Resolved by `PROJ-89` (vendor chunk split, 2026-07-29) and `PROJ-98` (PWA precache scope, 2026-08-02).** Precache is now 3.36MB (was ~19MB); no chunk exceeds 875KB raw. Removed from active backlog — re-add if bundle size regresses again.
* **Feature:** Crisis-Hotline Deep Link in SOS Modal.
  * **Concept:** A lightweight, opt-in deep link to a crisis hotline from the SOS Modal, matching a pattern seen in Loosid.
  * **Status:** Deferred — unscheduled. Surfaced 2026-08-31 by the finalreview audit (`docs/finalreview/14_EXECUTIVE_REPORT.md` #42); investigate whether it's worth adding, not yet committed.

## 👤 Ned (The Pink Cloud)
* **Feature:** "90 in 90" Meeting Tracker & Friend Challenges (PROJ-21).
  * **Concept:** Gamified attendance tracking.
  * **Complexity:** High (Requires secure multiplayer networking). Deferred to 5,000 user milestone.
* **Feature:** Sleep Log / Wearable Integration.
  * **Concept:** Apple HealthKit API integration to correlate sleep debt with cravings.
  * **Complexity:** Extremely High. Deferred to Wave 4.

## 👤 Lisa (The Service Superstar)
* **Feature:** Accountability Partner Mode.
  * **Concept:** A read-only "Listener" view where a sponsor can see a sponsee's clean time and public mood graph (without seeing encrypted journal entries).
  * **Status:** Deferred — unscheduled.
* **Feature:** Narrower ZK-compatible "Share Your Streak" Feature.
  * **Concept:** A smaller, faster-to-ship alternative/complement to the full Service Module (`PROJ-05`) — share just a streak with an accountability partner, without full sponsor/sponsee tooling.
  * **Status:** Deferred — unscheduled. Surfaced 2026-08-31 by the finalreview audit (`docs/finalreview/14_EXECUTIVE_REPORT.md` #39) as a possible faster complement to `PROJ-05`, which the audit separately calls the single highest-leverage recommendation in the report — see `docs/reports/2026-08_finalreview_synthesis_and_playstore_plan.md` §5. `PROJ-05` itself stays Paused; this is a distinct, smaller idea, not a substitute decision.

## 👤 Walt (The Zen Master)
* **Feature:** Photo Attachments in Journal.
  * **Complexity:** High (Requires Blob -> ArrayBuffer -> AES-GCM -> Base64). Deferred indefinitely.

## 🅿️ Parked / Unscheduled (Have specs, not on the Roadmap)
* **PROJ-38 — The Urge Intervention System ("The Lifeline Protocol").** Full spec exists at `docs/projects/38_URGE_INTERVENTION.md`. Status: ⚪ Planned, not yet scheduled into a Wave.
* **PROJ-45 — Adaptive Persona UI Engine.** Full spec exists at `docs/projects/45_ADAPTIVE_PERSONA_UI.md`. Status: ⚪ Planned — PARKED, explicitly not in active sprint pending the Open Questions in the spec's §6.
* **PROJ-97 Phase 3 — `crossword-layout-generator` replacement.** Full spec exists at `docs/projects/97_DEPENDENCY_HYGIENE_ROUND_2.md` (§6 Addendum). Everything else in that ticket shipped 2026-08-04 (`@use-gesture/react` replaced with native touch handlers, verified `knip` dead code removed, `eslint-plugin-jsx-a11y` added and all 48 findings triaged) — only this one piece is deliberately deferred. The package has zero CVEs and no newer release exists to bump to either way; replacing its grid-interlocking algorithm from scratch is multi-day, real-correctness-risk work for a working, low-traffic (one nightly Cloud Function run) feature. **Trigger:** the package becomes uninstallable (e.g. a Node engine bump it can't support), gets a real CVE, or Daily Crossword needs a capability only a rewrite could provide — not "the package is old" on its own.
* ~~**`PROJ-07` Sprint 9.2 — Google Play Console verification.**~~ **Cleared 2026-08-31** — account-holder confirmed both website ownership and identity/phone verification are complete. Moved to `docs/ROADMAP.md` Wave 0 / `docs/ACTIVE_CYCLE.md` as the actual Bubblewrap build/upload/submission step (still manual, account-holder-only — CLI + keystore + Play Console UI, not doable from an agent sandbox).
* **`mrt2-app-uat` — `VAULT_PEPPER` secret not set / Secret Manager API not enabled.** Surfaced 2026-07-19 while closing out `docs/projects/67_SIGNING_KEY_SECRETS_HYGIENE.md` §7 (prod and dev were both fixed in that pass). uat isn't currently in active use, so this is deferred rather than fixed now — but it will break the first Cloud Functions deploy to uat with the same `defineSecret("VAULT_PEPPER")` failure prod hit, until someone enables `secretmanager.googleapis.com` on `mrt2-app-uat` and runs `firebase functions:secrets:set VAULT_PEPPER --project=mrt2-app-uat` (plus the matching `roles/secretmanager.secretAccessor` IAM grant). No spec needed — this is a one-command chore, not a feature.

## 🔧 Infrastructure & Scale Triggers (No persona — internal readiness, no spec written yet)
*Surfaced by `docs/reports/2026-08_full_production_readiness_audit.md`'s Large-Refactor bucket (§20) and Scalability Review (§18). None are urgent at current traffic; each has a stated trigger condition below rather than a target Wave. Deliberately not spec'd yet per `CLAUDE.md`'s "no planning before a spec exists" rule — write the spec when the trigger is actually approaching, not speculatively now.*
* **`dailyBeacon` pub/sub fan-out rework.** The nightly notification function's own code comment already flags ~6,000 users (20 sequential batches) as its self-identified concern point; a separate, more urgent correctness bug in the same function (an unchunked `sendEach()` call past the SDK's 500-message limit) is tracked as its own item in `docs/projects/99_FIRESTORE_BACKEND_HARDENING.md` Phase 4, not deferred here. **Trigger:** approaching ~5,000-6,000 total users, or the first time a `dailyBeacon` timeout is observed in production logs, whichever comes first.
* **Aggregated Stats Engine (`PROJ-34`, already on `ROADMAP.md` Wave 3 as a one-line entry, no spec yet).** Cloud Functions to pre-compute streak/stat aggregates on-write, removing the full-history reads several client hooks currently perform (`getUserTasks`, `src/lib/insights.ts`, `JournalHistory.tsx`'s full-history mode). **Trigger:** the first real user complaint or measured slowdown attributable to full-history reads — the audit found the *pattern* but no evidence yet that it's actually hurting anyone at current scale.
* ~~**Feature:** Firebase App Check.~~ **Promoted 2026-08-31 — trigger met.** This item's own trigger ("proactively before a significant marketing/user-acquisition push") is met by the upcoming Play Store submission. Moved to `docs/ROADMAP.md` Wave 0 (Tier 1) — see `docs/reports/2026-08_finalreview_synthesis_and_playstore_plan.md`.
* **Bulk-migration framework** (paginated, resumable, Cloud-Function-driven — beyond the current single-`uid` admin button in `SchemaMigration.tsx`, which doesn't paginate past 500 documents for even one user). **Trigger:** the next genuinely breaking schema change that needs to apply to every existing account, not before — building this speculatively risks over-engineering for a shape of migration nobody has needed yet (`usesPepperV2`'s lazy per-account flag has covered every real migration so far).
* **Two still-open recommendations from `docs/reports/2026-07_firebase_architecture_review.md`** (surfaced 2026-08-04 governance audit — this report has zero citations elsewhere in `docs/`, so these were untracked until now): (1) `scripts/sync_security_rules.sh` can push directly to prod with no environment gate/confirmation prompt; (2) the transaction-vs-batch-write consistency model used across Firestore mutations isn't documented anywhere. ~~(3) `measurementId` dormant config~~ **done 2026-08-31** — removed entirely from `src/lib/firebase.ts`, `.env`/`.env.example`, and `.github/workflows/deploy.yml` (was never wired up; `firebase/analytics` was never imported, so no data was ever collected — PostHog is the analytics tool of record). **Trigger:** next time either remaining file is touched for an unrelated reason — not urgent enough to schedule proactively at current scale.
* **APM/distributed tracing + crash-reporting SDK** (Sentry or equivalent). Production performance and outages are effectively invisible until reported; no automatic incident alerting. **Trigger:** first genuinely hard-to-diagnose production incident, or proactively once user count grows enough that "find out from a user" stops being an acceptable incident-detection method — whichever comes first. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #11/#12.
* **DR/BC runbook, written and tabletop-tested.** `docs/RUNBOOK.md` exists (Hosting/Functions/rules rollback procedures, added under `PROJ-96`) but hasn't been read/verified against a documented RTO/RPO or actually tabletop-tested. **Trigger:** before any B2B/enterprise motion, or after the first real incident that exposes a gap in the existing runbook — whichever comes first. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #14.
* **Third-party penetration test.** No formal pentest evidence found; internal `zk-audit` skill reviews exist but aren't a substitute. **Trigger:** before any enterprise/B2B motion, or proactively before a significant marketing/user-acquisition push (same logic as the now-promoted App Check item) — whichever comes first. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #15.
* **Structured, moderated usability-testing pass** against each persona's documented worst-case scenario (`docs/PERSONAS.md`). **Trigger:** after the Play Store launch, once there's a real device install base to recruit testers from — not meaningful to run against the current dev-only environment. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #17.
* **Jordan (MAT persona) discreet-UI feature depth audit.** The audit flagged Jordan's discreet-UI promise (custom counter labels, one-tap dose logging, no drug names on lock-screen notifications) as stated but not fully traced/confirmed in code. **Trigger:** next time notification copy or the counter-labeling feature is touched for an unrelated reason, or proactively before marketing that specifically targets the MAT/Jordan segment. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #16.
* **Publish a consumer-facing "why zero-knowledge" trust page.** The ZK architecture was independently verified by the finalreview audit as real, not just claimed — currently not converted into a marketing/trust asset anywhere public-facing. **Trigger:** alongside or shortly after the Play Store launch, when there's real public traffic to show it to. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #18.
* **AI golden-answer eval suite for the 9 approved Gemini flows, plus re-tuning `generateAIInsights`'s `maxInstances`/concurrency limits against real traffic.** No eval suite exists today; concurrency limits (`PROJ-99`) were set without production traffic data to tune against. **Trigger:** first sign of AI-flow quality regression or concurrency-limit-related throttling in production logs, or proactively once there's real traffic data to tune against. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #20/#21.
* **Android IAP / external-link upgrade revisit.** `PROJ-68` sidesteps Play Billing by hiding in-app Stripe purchase inside the TWA — `docs/finalreview/13_FUTURE_STATE_ARCHITECTURE.md` §8 calls this "a compliance-adjacent workaround, not a long-term solution." **Updated 2026-09-01:** re-checked against live Play Console policy during PROJ-07 submission prep — the Epic v. Google landscape moved substantially since this item was first surfaced (formal **External Content Links Program** launched Dec 2025, March 2026 settlement, July 2026 joint withdrawal of further litigation suggesting stabilization, not the "expires 2027-11-01" framing originally assumed). Also found `PremiumUpgrade.tsx`'s "Upgrade on the Web" had shipped as a real clickable link (not the informational text the spec called for) — exactly the structured pattern that program now regulates (declaration, API integration, Play Console enrollment, 10-20% fees starting 2026-10-01). Fixed same day: converted to plain non-clickable text, which stays outside the program's scope entirely — see `docs/projects/68_STRIPE_TWA_GATING.md` §8. **Trigger (revised):** formally enrolling in the External Content Links Program to restore one-tap external upgrade is worth building only if Android-channel premium conversion becomes a proven, material business problem — not proactively. Re-verify Play policy again before any future Android billing decision; this space moves fast.
* **Surface `web-vitals`/PostHog data as an internal dashboard**, not just raw events. **Trigger:** after the Wave 0 "pull and review existing `web-vitals` data" chore lands (`docs/ROADMAP.md` Wave 0) — this is the natural follow-on once someone has actually looked at the raw data once. Surfaced 2026-08-31, `docs/finalreview/14_EXECUTIVE_REPORT.md` #49.
* **Capacitor-based iOS wrapper evaluation** (not commitment) — to close the push-notification reliability gap the PWA-only approach has on iOS. **Trigger:** a measurable pattern of iOS push-notification complaints/failures, or once the Play Store launch is stable enough to justify evaluating a second platform investment. Already named in the finalreview audit's own 12-month roadmap (`docs/finalreview/12_ROADMAP.md`).
* **Consolidate `CLAUDE.md`/`GEMINI.md` onto a single `AGENTS.md`.** Surfaced 2026-08-31 (`docs/reports/2026-08_ai_workflow_gap_analysis.md`) — confirmed real content drift between the two hand-maintained AI-instruction files (`GEMINI.md` was missing the vault-PIN exception, the Icons section, and the AI-sanitization framing; patched manually in that pass, but nothing prevents the next drift). `AGENTS.md` is now the actual cross-tool convention (formalized by OpenAI/Google/Cursor/Sourcegraph/Factory, donated to the Linux Foundation's Agentic AI Foundation Dec 2025), natively read by Antigravity alongside `GEMINI.md`, with Claude Code supporting `@import`. **Trigger:** next time `CLAUDE.md`/`GEMINI.md` are edited for an unrelated reason, or proactively if a third AI-tool config file (e.g. a `.cursorrules`) ever gets added to this repo — whichever comes first.
* **Harden `check_spec_quality.mjs` to structural (not just substring) validation, and migrate `governance/SKILL.md`'s one-off-script "APPLY ALL" pattern onto `sync_ticket_docs.py`'s reusable-script precedent.** Surfaced 2026-08-31 (`docs/reports/2026-08_ai_workflow_gap_analysis.md`) — confirmed two specs (`docs/projects/26_THE_BEACON.md`, `42_DAILY_READINGS.md`) structurally diverge from `00_TEMPLATE.md`'s header/section shape yet still pass the CI gate, because the checks are keyword-substring-only. **Trigger:** the next spec-quality false-negative caught in manual review, or opportunistically alongside other docs-as-code tooling work.
* **Evaluate the official Firebase MCP server + Agent Skills for Firebase.** Google shipped both (Feb 2026) for live introspection of deployed rules/functions/App Check state directly from Claude Code/Antigravity/VS Code Copilot — this repo currently has zero MCP servers configured. Surfaced 2026-08-31 (`docs/reports/2026-08_ai_workflow_gap_analysis.md`). **Trigger:** the next time debugging a live Firebase-state mismatch (rules/functions/App Check config drift between `mrt2-app-dev`/`-uat`/`-prod`) takes real time via manual `firebase` CLI round-trips — opt in reactively then, not proactively now, given MCP tool definitions carry a permanent context-token cost whether used or not.

## 🧰 P2 Hygiene — Opportunistic (Pick Up Between Major Work, No Trigger Needed)
*Low-effort, low-urgency items from the 2026-08-29 finalreview audit (`docs/finalreview/14_EXECUTIVE_REPORT.md` #26-#29, #31-#33, #35-#36) that don't warrant a scheduled Wave slot or a specific trigger condition — just pick up opportunistically alongside other work in the same area.*
* Resolve the `pdf-export`/`vendor` circular-chunk build warning (`TD-12`) — next time `vite.config.ts` manual chunks are touched.
* Adopt Prettier alongside the existing flat ESLint config.
* Run `knip`/`npm run deadcode` as a periodic CI check, not just an available script.
* Add a Contributing/PR-process section to the README.
* Add a dedicated Lighthouse-CI budget gate to the CI pipeline.
* Add a preview-channel deploy step ahead of UAT promotion.
* Refactor the 13 files that bypass the hooks-only Firestore-access convention into `src/hooks/` (`TD-04`) — incrementally, as each file is touched anyway.
* Add dynamic/latest-model routing with a fallback chain for Gemini model selection.
* Formalize the prompt-injection delimiting pattern (already shipped across all 9 flows under `PROJ-100`) as a reusable, tested utility for any future AI call site.
* **Reconcile `docs/legal/PRIVACY_POLICY.md` and `docs-site/privacy.md`.** Discovered 2026-08-31 while drafting the sub-processor list: these are two independently-maintained copies of the same policy that have drifted from each other (different wording throughout, `docs-site/privacy.md` is missing the "Service Data" mention `docs/legal/PRIVACY_POLICY.md` has, etc.) — `docs-site/privacy.md` is the one actually linked from the app (`Login.tsx`/`Profile.tsx`) and therefore the operative one. Both were patched with the same missing third-party disclosures (PostHog/Stripe/Google Drive) today, but the deeper drift between them wasn't otherwise touched. Pick one as the source of truth (`docs-site/privacy.md`, since it's the one users actually see) and either delete or clearly mark the other as non-authoritative.

## 🔭 Contingent / Strategic (No Action Unless a Specific Signal Appears)
*Deliberately not spec'd or scheduled — each of these is a "decide later, informed by data we don't have yet" item per the finalreview audit's own framing, not a default extension of the roadmap.*
* **Multi-region Firestore configuration.** Evaluate only if international expansion beyond the current AA/NA/SMART/Recovery-Dharma fellowship base is being considered — not a default priority. `docs/finalreview/14_EXECUTIVE_REPORT.md` #37.
* **Full i18n rollout.** Zero i18n infrastructure today (`TD-19`); evaluate only if a specific non-English market opportunity is identified — a full string-externalization pass across the entire app is expensive enough that it shouldn't be spec'd speculatively. `docs/finalreview/14_EXECUTIVE_REPORT.md` #38.
* **B2B/enterprise motion (SOC 2 Type I readiness, SSO/multi-tenant admin scoping).** A genuine strategic fork, not a default extension of the current consumer roadmap — the audit explicitly does not recommend a direction. If pursued, begin SOC 2 Type I readiness work early (the existing CI/documentation discipline is a strong foundation) and scope SSO/multi-tenant requirements before committing to a sales timeline. `docs/finalreview/14_EXECUTIVE_REPORT.md` #45/#46.
