# 📁 Project 108: Persona Asset Refresh (Pop-Art Style Adoption)

**Status:** ⚪ Planned — paused before implementation (2026-09-03) pending an in-progress AI image-generation capability being added to the project; resume planning review once that lands, since it may change Strategy B/C's feasibility (e.g. generating true pop-art-style bio art instead of only cropping/compressing the provided reference PNGs).
**Primary Persona:** N/A — Marketing/brand asset surface (`Welcome.tsx`, `Login.tsx`). No single end-user persona owns this; it's how MRT presents all six personas to prospective users before signup.
**Objective:** Replace the persona headshot/bio imagery across the app with the new pop-art/comic-style reference set delivered in `public/personas/Persona Headshots and Angles/`, resolving two pre-existing style inconsistencies (David/Ned/Lisa/Walt's flat-cartoon style vs. Maya/Jordan's mismatched corporate-avatar style) and the long-standing Maya/Jordan bio-content gap, in a single coherent pass.

---

## 1. The Executive Summary
**User Story:** As a prospective user landing on the Welcome page (or an existing user on `/login`), I want the six persona illustrations to look like one deliberate, cohesive art style so that MRT reads as a polished, intentional product rather than a set of mismatched stock-style avatars stitched together over time.
**Competitive Gap:** Persona-driven onboarding is a differentiator vs. generic sobriety trackers ("I Am Sober", "Reframe") — but only if the personas feel like a designed cast, not placeholder art. The current set has three competing illustration styles across six people; this closes that gap and gives MRT a distinctive, ownable visual identity (bold pop-art/halftone) instead of generic flat-cartoon icons.

---

## 2. Security & Zero-Knowledge Audit 🛡️
* [x] **Data Sensitivity:** Does this feature handle PII or emotional data? **No.** Static marketing/UI illustration assets only — fictional personas, not real users.
* [x] **Encryption Strategy:** Will this use `src/lib/crypto.ts`? **No.**
* [x] **Key Rotation:** Does this data need to be included in `executePinRotation`? **No.**

---

## 3. Schema & Architecture 🗄️

**Source material (already in repo, not yet web-ready):**
`public/personas/Persona Headshots and Angles/mrt_<name>_{headshot,front_view,360_view}.png` — 18 files, 6 personas × 3 angles, ~2–3MB each, uncropped/uncompressed production renders. These are **inputs to be processed**, not final web assets — they must not be served from `public/` at full size.

**Pre-existing, never-implemented structure this refresh should finally satisfy:** `docs/PERSONAS.md`'s per-persona "Asset Metadata" sections have referenced `ASSETS.personas.<name>.full_body` and `ASSETS.personas.<name>.looking_left` since at least v2.1 — but `src/data/assets.ts` (auto-generated by `scripts/generate_asset_index.py`) has never actually contained those keys, only flat `<name>_headshot` / `<name>_bio` / `<name>.jpg` entries. The new `front_view` (full body) and `360_view` (3-pose turnaround, includes a left-profile pose) source images are a natural fit for finally implementing `full_body` and `looking_left` — this refresh should close that gap rather than perpetuate it.

**Target directory structure** (mirrors the nested-object shape `docs/projects/24_ASSET_ENGINE.md` originally specified but was never fully realized — current `assets.ts` is flat per-persona keys, not nested):
```
public/personas/
  david/headshot.webp
  david/full_body.webp
  david/looking_left.webp
  ... (×6 personas)
```
Restructuring from flat (`david_headshot.webp`) to nested (`david/headshot.webp`) is a **decision point for `/planning`**, not assumed here — it changes every call site (`ASSETS.personas.david_headshot` → `ASSETS.personas.david.headshot`) and touches `Welcome.tsx`, `Login.tsx`, and `docs/PERSONAS.md`'s own documented (if never-implemented) shape. Either the nested shape ships now (fulfilling the long-documented intent) or the flat convention is kept and `PERSONAS.md`'s Asset Metadata sections are corrected to match reality instead — `/planning` should weigh both as strategies.

**Bio card format — the actual open problem:** The current `*_bio.webp` files are pre-baked composite images (headshot + backstory text burned into the pixels by whatever tool made them) — and are dead code today (registered in `assets.ts`, referenced nowhere in `src/`). The new source set contains **no equivalent bio-card composites**, only headshot/body/turnaround art. This session has no image-generation tool available, so a byte-for-byte "new pop-art bio card" cannot be produced the way the old one was. The realistic path is converting the bio card from a pre-baked image into a **code-rendered component** (new pop-art headshot crop + a styled text panel built with Tailwind, matching the halftone/bold-outline palette) — this also directly solves the Maya/Jordan bio-content gap, since bio text can be authored without needing matching baked artwork. `/planning` should confirm this approach and design the component.

**Files needing updates:**
* `src/data/assets.ts` — regenerated via `python3 scripts/generate_asset_index.py` after source files are processed and placed (never hand-edited, per its own header).
* `src/pages/Welcome.tsx` — `PERSONA_CONTENT` array, `headshot:` fields (4 of 6 personas currently; decide whether to expand to all 6 or leave Maya/Jordan absent from this specific marketing carousel, which is itself an open product question, not an asset question).
* `src/pages/Login.tsx` — `PERSONAS` array currently hardcodes `/personas/<name>.jpg` (bypasses `ASSETS` entirely, and those jpgs are actually full bio-card composites mis-served as plain avatars). Needs to both switch to real headshot crops and route through `ASSETS` instead of hardcoded strings — a small pre-existing bug this refresh should fix in passing.
* `docs/PERSONAS.md` — Asset Metadata sections per persona, once the real key shape is decided (see above).
* Old files retired: `david|ned|lisa|walt_headshot.webp`, `*_bio.webp`, `*.jpg`, `maya|jordan_headshot.webp` — deletions, not silent overwrites; confirm nothing else references them first (already checked: only `assets.ts`, `Welcome.tsx`, `Login.tsx` do).

**No Firestore/db changes.**

---

## 4. Implementation Phases 🏗️

### Phase 1: Source Processing (no code changes)
* Crop/compress the 6 `_headshot.png` files to square, web-ready WebP (target similar file size to current ~10–40KB headshots, not the current 2-3MB raw PNGs).
* Crop/compress the 6 `_front_view.png` files → `full_body.webp`.
* Decide 360-view handling: crop a single left-profile panel per persona → `looking_left.webp`; the full 3-pose sheet stays as unoptimized reference material outside `public/` (e.g. a `reference/` or `_source/` folder, or removed from the repo entirely if not needed post-crop) rather than shipped to users.
* Place processed files per the directory-shape decision from §3.

### Phase 2: Generator & Registry
* Run `scripts/generate_asset_index.py` to regenerate `assets.ts` — never hand-edit.
* Verify no stale key references remain (`grep -rn "ASSETS.personas" src/`).

### Phase 3: Component Integration
* `Welcome.tsx`: swap `headshot:` sources; decide Maya/Jordan inclusion.
* `Login.tsx`: fix hardcoded path bug, route through `ASSETS`, use real headshots not bio-card composites.
* New bio-panel component (replaces `*_bio.webp` composite pattern): headshot + code-rendered text, styled to match pop-art palette. Author Maya/Jordan bio copy from their existing `docs/PERSONAS.md` narrative sections (backstory already written there — just needs condensing into the card's Backstory/Current Stage/Key Challenge format the other four use).
* **Somatic Check:** None of this touches David's crisis flows — pure marketing/onboarding surface, no risk to the crisis-first UX floor.

### Phase 4: Cleanup
* Delete retired source files (old webp/jpg) once new ones are confirmed working — not before.
* Correct `docs/PERSONAS.md` Asset Metadata sections to match implemented reality.

---

## 5. QA & Verification 🧪
* [ ] **Visual regression:** `npm run build` + manual check of `/` (Welcome) and `/login` in browser — confirm no broken image paths, no layout shift from new aspect ratios.
* [ ] **404 check:** No `ASSETS.personas.*` reference in `src/` should point to a path that doesn't exist in `public/personas/` post-refresh.
* [ ] **The 320px test:** New headshot crops must not be distorted/cropped badly at iPhone SE width in `Welcome.tsx`'s persona carousel.
* [ ] **Unit test:** `assets.test.ts` (if it exists) still passes with the new key shape.

---

## 6. Planning Protocol Output (2026-09-03) — AWAITING APPROVAL

Ran through `/planning`. Full detail below; paused at the Stop Gate before the user typed APPROVED because a new AI image-generation capability is being set up for the project (see PROJ-109) that may change what's feasible here — e.g. generating genuinely new pop-art-style art (bio panels, missing angles) instead of only cropping/compressing the 18 reference PNGs already in `public/personas/Persona Headshots and Angles/`. **Re-open this section and re-evaluate Strategy B vs. C before resuming** — image-gen may make Strategy C (or a new Strategy D built around generation rather than cropping) cost-competitive with B.

### Phase 1: Dependency Impact Table
| File/Module | Type | Impact | Confidence |
|---|---|---|---|
| `public/personas/Persona Headshots and Angles/*.png` (18 files) | Source | READ | HIGH |
| `public/personas/*_headshot.webp`, `*_bio.webp`, `*.jpg` (14 files) | Asset | MODIFY (retire/delete) | HIGH |
| `scripts/generate_asset_index.py` | Script | NO-CHANGE (run as-is) | HIGH |
| `scripts/optimize_assets.py` | Script | UNCERTAIN — built for `raw_assets/`→`Marketing/` with different naming/routing; a new one-off crop+compress script is likely needed | LOW |
| `src/data/assets.ts` | Generated | MODIFY (regenerated, never hand-edited) | HIGH |
| `src/pages/Welcome.tsx` | Component | MODIFY — `PERSONA_CONTENT` headshot sources; possible Maya/Jordan carousel entries | HIGH |
| `src/pages/Login.tsx` | Component | MODIFY — fix hardcoded `/personas/*.jpg` paths (bug: currently serves bio-card composites as avatars), route through `ASSETS` | HIGH |
| `docs/PERSONAS.md` | Docs | MODIFY — Asset Metadata sections, 6 personas | HIGH |
| `docs/projects/24_ASSET_ENGINE.md` | Docs | NO-CHANGE (historical record) | HIGH |
| `assets.test.ts` | Test | N/A — does not exist in repo today | HIGH |
| Bio-card/panel component | Component | NOT NEEDED as originally scoped — confirmed by reading `Welcome.tsx` lines 218-250: the live carousel only renders a small circular headshot avatar + quote + screenshot. The `_bio.webp` composite-card concept is dead code today, not a live UI surface | HIGH |

### Phase 2: Three Strategies
**A — Headshot-only swap, flat naming kept.** Crop/compress 6 headshots, keep flat filenames, swap into `Welcome.tsx`, fix `Login.tsx`, delete old files. No `full_body`/`looking_left`, no `PERSONAS.md` changes. Effort 0.5-1 day. David/Walt: no exposure (pre-vault marketing page). Scores: speed 5 / persona 3 / ZK 5 / maintenance 3 / test surface 5.

**B — Full angle set, nested structure, Maya/Jordan parity (recommended pre-image-gen).** Crop/compress all 18 PNGs into `public/personas/<name>/{headshot,full_body,looking_left}.webp`, finally implementing the `full_body`/`looking_left` keys `PERSONAS.md` has documented since v2.1 but `assets.ts` never had. Expand `Welcome.tsx` carousel to all 6 personas. Fix `Login.tsx`. Correct `PERSONAS.md`. Delete old files. No new bio component. Effort 1.5-2 days. Scores: speed 3 / persona 4 / ZK 5 / maintenance 5 / test surface 4.

**C — B + new code-rendered bio-card component.** Adds a Tailwind bio panel (headshot + styled backstory text) replacing the dead `_bio.webp` concept, with authored Maya/Jordan backstory copy. Effort 3-4 days. Speculative — no current consumer needs a bio card. Scores: speed 2 / persona 4 / ZK 5 / maintenance 5 / test surface 3.

**Recommendation (pre-image-gen): Strategy B.** Closes both flagged gaps without inventing unused UI. **Reassess once image-gen is available** — it may unlock true bio-card artwork generation, making C viable at lower effort than hand-building the component's visual polish, or enable a Strategy D that generates fresh art for angles/poses instead of just cropping the provided reference sheet.

### Phase 3: Technical Impact
1. **Schema changes:** None — static assets only, no `db.ts` changes.
2. **Firestore rules/indexes/Cloud Functions:** None.
3. **Metadata fields to preserve:** N/A.
4. **Date normalization:** N/A.
5. **ZK boundary:** No encrypted fields — public marketing illustrations, never through `crypto.ts`.
6. **Test contract:** Unit — none exist; optional smoke test for `ASSETS.personas.<name>.headshot` across all 6. Integration — manual browser check of `/` and `/login`. Security — N/A. Regression — `npm run lint` + `npm run build` + `grep -rn "ASSETS.personas" src/` for stale keys.
7. **Bundle check:** Zero — images served by URL from `public/`, never imported into JS bundle. No new deps/routes/Gemini calls (for Strategy A/B; an image-gen-based approach would need this reassessed).
8. **Rollback:** Fully `git revert`-able — no server-side state, no deploy, no migration.

**Stop Gate: PAUSED, not approved.** Resume by re-reading this section once PROJ-109 (image-gen setup) lands, then either type APPROVED for Strategy B as-is or ask to re-run `/planning` with image-gen in scope.
